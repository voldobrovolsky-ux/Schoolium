# Consumer contracts

Schoolium and each other relying party must publish a consumer contract here (or in its own repository and register it here) before the IDP is accepted as Tier 0. Contracts must cover OIDC token claims, group API access isolation, and webhook idempotency.

No Pact is generated yet because there is no implementation. This directory is deliberately present so CI can turn verification into a deployment gate when consumers exist.
