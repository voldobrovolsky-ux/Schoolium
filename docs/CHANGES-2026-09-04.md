---
id: SCHOOLIUM-CHANGES-150
title: Пакет 1.5.0 — правки по итогам созвона владельца с завучем (04.09)
status: Accepted
owner: владелец продукта
last_reviewed: 2026-09-04
---

# Пакет 1.5.0 — что изменилось и почему

Одна сессия, шесть пунктов владельца и завуча (созвон 2026-09-03), один пакет.
Документ — **реестр правок**: что сделано, каким решением закреплено, какими
воротами доказано, что осталось узлом. Решения — `docs/ar/*.md` (AR-199…AR-210),
ворота — `docs/G-CHECKS.md`, экраны — `specs/school-onboarding/70-screens.md`.

## 0. Что просил владелец и что получил

| # | Задание | Результат | Решения | Ворота |
|---|---|---|---|---|
| 1 | СанПиН блокирует генерацию — убрать полностью, школа полного дня | Три отказа норм (`LOAD_EXCEEDS_SANPIN`, `DAY_EXCEEDS_SANPIN`, `DAY_TOO_LONG`) не срабатывают ни в нормах часов, ни в параметрах дня, ни перед перебором — ни со скелетом, ни без; вместимость дня любого класса = «уроков в день» школы; подсказки норм в `S-41.4` сняты, «уроков в день» до 12. Коды остаются в контракте как выведенные из употребления | AR-199 (вытесняет AR-103, AR-114) | G-34, G-46 переписаны |
| 2 | Обед не у всех после одного урока — дифференциация на каждый класс | `SchoolClass.lunchAfterLessonNo`: класс с «после N-го» получает обед в позиции N+1, генератор её не занимает и не считает окном; блок «Обед по классам» в скелете дня (`S-41.lunch`), строка «Обед» в дне класса и в дневнике; смена обеда роняет сетку в `stale` | AR-200 | G-86 (`lunch:check`) |
| 3 | «алгебра» и «Алгебра» созданы как два предмета — объединить; раздел «Предметы» по дисциплинам/по классам с флажками; группы в компетенциях; незаполненные — жёлтым | Ключ имени без регистра и пробелов (`nameKey`), `SUBJECT_EXISTS` при дубле, пресет и импорт идемпотентны по ключу; слияние дублей — `subjects:merge --workspace [--apply]` (действие `subjects-merge` workflow, dry-run по умолчанию); фильтр «По классам» раскрывает ряд флажков классов (`S-20.chips.classes`), незаполненные позиции — фон предупреждения + бейдж; в `M-25` — «по группам», выбор группы, смена числа групп класса (`PUT /classes/:id/groups`) | AR-201, AR-202, AR-208 (узел AR-210) | G-85 (`subjects:check`), G-70, G-78 переписаны, G-53/G-55 |
| 4 | Карточка сотрудника на весь экран; видят все, действуют админ и модератор; ФИО, логин, пароль плашками; ссылка на профиль; ссылка входа с меню срока и числа использований; QR; 6-значный код; роли и предметы | `M-06` полноэкранная на обеих раскладках; плашки ФИО/логин/пароль, правка ФИО и логина (`PUT /staff/:id/account`), «Задать пароль» (`M-32`, `POST /staff/:id/password`; текущий пароль не хранится и не показывается — AR-156); ссылка входа с выбором 24 ч / 48 ч / 7 дней и 1 / 3 / 10 / без лимита открытий, выпускают `staff.manage` (модератор и администратор); таймер у кода; группа «Предметы» | AR-203, AR-204 (вытесняет AR-189, AR-195) | G-87 (`staffcard:check`), G-81 переписан |
| 5 | В «Политиках» рядом с лимитом сессий — лимит ролей школы | `SchoolAccessPolicy.roleLimits`; дефолты 1/1/1 у директора и замов сохраняют прежние синглтоны; носители = членства + пустые слоты; отказ `ROLE_LIMIT_REACHED` с ролью и цифрами; трёхколоночная сетка в `S-62.policy.limits` | AR-205 (уточняет AR-182) | G-43, G-81 переписаны |
| 6 | У педагога в «Расписании» — «Мои предпочтения» (дни работы для генератора) и «Отмена урока» с причиной и автозаменой | `TeacherPreference.workDays` + `M-30`; генератор не ставит урок вне рабочих дней, `TEACHER_DAYS_SHORT` до перебора; `M-31` «Отмена урока»: свободный педагог той же дисциплины назначается автоматически (`Замена: …`, журнал пускает его), иначе урок отменён и строителю/завучу видна плашка «Уроков без замены: N» с ручным назначением; отзыв отмены | AR-206 (уточняет AR-135), AR-207 (вытесняет AR-145; узел AR-209) | G-88 (`teacher:check`), G-50, G-35 |

## 1. Как читать изменения по слоям

### Контракт (`packages/shared/src/schoolium.ts`)
- Права: `schedule.preference.self`, `lesson.cancel.self` — пакет `teacher`.
- Коды отказа: `SUBJECT_EXISTS`, `GROUPS_BOUND`, `ROLE_LIMIT_REACHED`,
  `LINK_EXHAUSTED`, `TEACHER_DAYS_SHORT`, `NOT_YOUR_LESSON`,
  `LESSON_ALREADY_HELD`, `LESSON_CANCELLED`, `SUBSTITUTE_BUSY`; три кода
  СанПиН выведены из употребления; `ARITHMETIC_REFUSALS` — шесть.
- Функции: `subjectNameKey`, `canonicalSubjectName`, `effectiveRoleLimit`
  с `DEFAULT_ROLE_LIMITS`; словари `LESSON_CANCEL_REASONS`,
  `ACCESS_PARAMS.loginLinkTtlOptions/loginLinkUsesOptions`.
- DTO: обед класса, группы класса, учётка и пароль сотрудника, ссылка входа с
  параметрами, политика с `roleLimits`/`roleHolders`, предпочтения педагога,
  датированные уроки с заменой, отмена/назначение замены, дневник с
  `cancelled`/`substituteName`.

### База (`apps/api/prisma`)
Одна миграция `20260904090000_deputy_pack_0409`, только расширение:
`SchoolClass.lunchAfterLessonNo`, `SchoolSubject.nameKey` (с бэкфиллом и
обычным индексом — уникальный индекс добавится после слияния дублей на
проде), `BootstrapLink.maxUses/useCount`, `SchoolAccessPolicy.roleLimits`,
`JournalColumn.cancelledAt`, таблицы `TeacherPreference` и `LessonSubstitution`
под tenant-guard. Откат на прежний тег остаётся возможным: старый код лишних
колонок не читает. Применяет контейнер API при старте.

### События (`apps/api/src/schoolium/schoolium.contract.ts`)
32 события (было 25): `schedule.lesson.cancelled.v1`,
`schedule.lesson.reassigned.v1`, `schedule.lesson.restored.v1`,
`schedule.preference.set.v1`, `staff.account.updated.v1`,
`staff.password.set.v1`, `contingent.class.groups.changed.v1`; у каждого —
издатель, подписчик, реакция и подпись аудита; `school.policy.set.v1` несёт
`roleLimits`, `staff.login_link.issued.v1` — срок и лимит открытий.

### API (`apps/api/src/schoolium`)
- Расписание: `PUT /schedule/lunch`, `GET/PUT /schedule/preferences/me`,
  `GET /schedule/preferences`, `GET /schedule/lessons?from&to`; генератор без
  норм СанПиН, с обедом класса и рабочими днями педагога.
- Уроки: `POST/DELETE /lessons/:id/cancel`, `POST /lessons/:id/substitute`
  (`SubstitutionService`); журнал переставляет педагога колонки по событию и
  отклоняет отметку в отменённый урок.
- Предметы: ключ имени, `SUBJECT_EXISTS`, компетенции с группами,
  `PUT /classes/:id/groups`; скрипт `school-subjects-merge.ts`.
- Персонал: `PUT /staff/:id/account`, `POST /staff/:id/password`, ссылка
  входа с параметрами под `staff.manage`; лимиты носителей ролей в
  `addCard`/`addRole`/`reactivate`; политика с `roleLimits`.

### Web (`apps/web/src/schoolium`)
- `ui.tsx`: `Modal desktop="fullscreen"`.
- `S-20`: ряд флажков классов, форматы строк с группами, подсветка
  незаполненных; `M-25`: группы.
- `S-31`/`M-06`: полноэкранная карточка, плашки и редактирование, `M-32`,
  селекты ссылки, таймер, предметы.
- `S-40`/`S-41`: обед по классам, дни педагога, `M-30`, `M-31`, маркеры
  отмены/замены, плашка без замены, назначение замены; дневник — «Урок
  отменён» / «Замена: …».
- `S-62`: третья колонка лимитов ролей.

### Реестры и документы
`docs/ar/domain.md`, `roles.md`, `ui.md` (AR-199…AR-210), `INDEX.md`
(`next=211`), `G-CHECKS.md` (G-85…G-88), `70-screens.md`, `75-adaptive.md`,
`30-spec.md`, `10-basis.md`, `docs/CABINETS.md`, `docs/CHECKLIST-UTC-KPC.md`,
`docs/UTC-BOUNDARY.md`, `docs/DEPLOY.md`, `.github/workflows/deploy-school.yml`.

## 2. Узлы владельца (не угадано)

- **AR-209** — допускать ли замену педагогом другой дисциплины (присмотр),
  когда педагога того же предмета в слоте нет. Пока — урок отменяется и
  показывается «Уроков без замены».
- **AR-210** — нужно ли собственное разбиение учеников по предмету, отличное
  от групп класса. Пока — группы класса (AR-75).
- Слияние дублей на проде выполняется только после dry-run по логу прогона
  workflow; Д6-конфликты между дублями не сливаются автоматически.

## 3. Чего в пакете нет и почему

- Уникальный индекс по ключу имени предмета — второй миграцией после
  слияния прода (миграция с уникальностью упала бы на несведённых дублях, а
  применяет её контейнер API при старте).
- Разные времена уроков у ступеней (полный скелет на ступень) — остаётся
  слотом AR-171; обед по классам сделан на общей сетке.
- Долг часов и добор (AR-145) — слот блока «Расписание»; отмена урока без
  замены час не добирает.
- Утверждение предпочтений директором (AR-135) — не делается: рабочие дни
  педагог задаёт сам.

## 4. Свидетельства

Заполняется по итогам прогона: команды и числа ворот, ссылки на прогоны CI и
деплоя — см. §5.

## 5. Итог прогона

_Заполняется перед мержем._
