<!-- ar-register: id=ui; ranges=40-43,69,77,80-82,90,95,105,113,115,161,167,169,175,181,184-185,190-192,197 -->

# АР-реестр · Интерфейс и клиент

> Блок ведения: **Интерфейс и клиент**. Владелец: фронтенд. Диапазоны номеров: `40-43,69,77,80-82,90,95,105,113,115,161,167,169,175,181,184-185,190-192,197`.
> Нумерация сквозная по всей системе — см. [AR-PROTOCOL.md](../method/AR-PROTOCOL.md).
> Индекс всех реестров: [INDEX.md](./INDEX.md). Ворота: [G-CHECKS.md](../G-CHECKS.md). Линзы: [LENSES.md](../method/LENSES.md).

| № | Решение | Статус | Ворота | Линзы |
|---|---|---|---|---|
| AR-40 | **PWA-минимум** (решение владельца 2026-07-28): иконки добавляются; SW остаётся для кеша чтения (школьный Wi-Fi нестабилен); офлайн-мутации — честный запрет: молчаливый optimistic в мобильном журнале заменяется откатом с тостом «нет сети — оценка не сохранена». Полноценная офлайн-очередь — слот. | [РЕШЕНО] | нет: ручной прогон PWA | L-12, L-15 |
| AR-41 | **URL-роутинг фронта**: сейчас query-параметры + useState, deep-link только `?pilot=`/`?bind=`. Дефолт: раздел/класс/вкладка отражаются в URL (history), без ввода тяжёлого роутера. | [дефолт] | нет: дефолт | L-15, L-14 |
| AR-42 | **Порог confidence ASR**: фронт получает `confidence`, но не использует. Дефолт: ниже порога (напр. 0.55) — принудительная дизамбигуация/ручной ввод; порог настраиваемый. | [дефолт] | нет: дефолт | L-12, L-16 |
| AR-43 | Дублирование логики фронта (journal context vs useMobileJournal; VoiceOverlay vs MVoice; две дизайн-системы `design/` vs `admin/ds/`). Дефолт: единый источник на пару при следующем касании кода; вторая ДС легализуется решением или сливается. | [дефолт] | нет: дефолт | L-15, L-10 |
| AR-69 | **Экраны общие, действия гейтятся**: все шесть ролей v1 видят кабинеты календаря, расписания и журнала; различие ролей выражается доступностью **действий**, а не скрытием экранов. Право проверяется с первого дня, каталог прав пополняется в том же коммите (AR-35) — ретро-гейтинг ломает уже сложившиеся привычки пользователей и стоит дороже. Кабинет управления школой виден только модератору. | [РЕШЕНО] | G-10 | L-15, L-8 |
| AR-77 | **Единый паттерн кабинета модератора**: экран — сетка карточек; открытие — блюр фона + карточка в центре; действия — на открытой карточке. Опциональные параметры мастеров выбираются явно, включая отказ: кнопка «без литер» / «без групп», пустое поле выбором не считается (следствие AR-18). Превью произведения «параллели × литеры = N классов» показывается до подтверждения. | [РЕШЕНО] | G-33 | L-15, L-14 |
| AR-80 | **Дизайн-направление Schoolium** (решение владельца 2026-08-20): фиолетово-розовая гамма с обилием белого. Токены — `docs/design/tokens.json`, контраст всех пар «текст × фон» доказывается перечислением (G-37, WCAG 2.1: текст ≥4.5, UI ≥3.0). **Правило разведения**: брендовый розовый живёт только в интерактиве (кнопки, акценты, бейджи) и никогда не заливает строки данных; пастель маркеров пола — только в журнале контингента (проверка G-37 показала: заливки почти совпадают, различать их обязаны контекст и форма, не оттенок). Смысл не кодируется одним цветом: у отметок есть символ, у маркеров пола — позиция строки. | [РЕШЕНО] | G-37 | L-15, L-16 |
| AR-81 | **Навигационная оболочка — две раскладки, одна структура**: десктоп — постоянный левый сайдбар 240px (сворачиваемый до 72) + топбар 64px; мобайл — хедер 56px + нижний таб-бар из **пяти вкладок, одинаковых для всех шести ролей** (Классы · Предметы · Персонал · Расписание · Журнал). Кабинет модератора не становится шестой вкладкой и не создаёт отдельного меню: различие ролей выражается доступностью действий внутри экранов (AR-69), иначе два человека, обсуждая продукт, видят разные меню. Экраны входа показываются без оболочки. | [РЕШЕНО] | G-39 | L-15, L-14 |
| AR-82 | **Правила модалок**: вложенность не глубже двух уровней (карточка → QR-слой); на мобайле мастера и карточки — полноэкранные, выборы и поповеры — нижние листы; выход из мастера с введёнными данными требует подтверждения; ловушка фокуса и возврат фокуса обязательны. Третий уровень вложенности — признак того, что поток спроектирован неверно, а не сложного случая. | [РЕШЕНО] | G-39 | L-15, L-2 |
| AR-90 | **У каждой операции есть обратная либо названная причина её отсутствия**: реестр обратимости в `30-spec.md` перечисляет операции версии парами «действие → выход назад»; пустая пара — дефект спеки, а не «редкий случай». Причина, когда обратной нет, формулируется механикой, а не удобством: «удаление доступно только записи без отметок, цена ошибки равна повторному вводу» — причина; «пользователь вряд ли захочет» — нет. Правило вводится реестром, потому что необратимые операции появляются по одной и каждая по отдельности выглядит безобидно. | [РЕШЕНО] | G-43 | L-9, L-14 |
| AR-95 | **EduStore пилота — сайт, а не приложение**: лендинг на корне домена с единственной кнопкой «Вход»; **кнопки регистрации на сайте нет и не будет** — Schoolium мультитенантен, пользователей заводит школа (QR модератора), самостоятельной регистрации не существует как класса. Карта маршрутов и редиректов — раздел «Карта сайта» в `30-spec.md`: аноним на маршруте приложения → `/login?next=<путь>` с возвратом после входа; вошедший на `/login` или по кнопке «Вход» → стартовый экран роли; `/join/:token` под живой чужой сессией → предупреждение, а не молчаливая регистрация второго человека в том же браузере. Полноценное приложение (PWA-обвязка и далее) — по мере роста, лендинг и вход не меняются. | [РЕШЕНО] | G-38 | L-14, L-15 |

| AR-105 | **Разрушающая операция стоит в реестре обратимости своей строкой, а подтверждение называет действительный объём потери.** Реестр AR-90 перечислял разрушение только как «обратную» колонку к созданию, и четыре операции не имели строки вовсе: удалить класс, удалить предмет, снять роль, открепить педагога. Две из них необратимы — необратимых операций в версии не три, а **пять**. Опаснее прочего удаление класса: условие — «ни у одного ученика нет отметок», а до состояния `ready` отметок не существует **вовсе**, значит весь онбординг любой класс удаляется одной кнопкой вместе с профилями учеников. Текст подтверждения `M-13` при этом обещал «15 **пустых** профилей будут удалены» — то есть в единственном опасном случае (профили заполнены, отметок ещё нет) модалка сообщала человеку обратное тому, что происходит. Правило: подтверждение удаления класса называет **заполненные** профили отдельно от пустых («Удалить 5А? Заполнено 15 профилей из 15 — они будут удалены»), а кнопка подтверждения в этом случае `B-danger`, а не primary. Детектор линзы L-9 расширен: перечисляются не только создающие операции, но и разрушающие (свойство P12). | [РЕШЕНО] | G-48 | L-9, L-15 |

| AR-113 | **У отказа «удаление записи, у которой есть история» есть имя — два, по числу объектов.** Правило подмены кнопки решает сервер (AR-78, AR-89), но гейт живёт в контракте, а не в интерфейсе (красная линия 3), и случай, когда интерфейс показывает «Удалить», а сервер обязан отказать, — не теоретический: модератор открыл карточку при `hasMarks: false`, педагог в этот момент поставил отметку, модератор нажал кнопку. Это ровно та конкурентность, которую AR-109 объявил штатной. Подменять отказ кодом `CLASS_HAS_MARKS` нельзя — тот про класс и текст его про класс; оставлять безымянным нельзя — §9 требует, чтобы каждый видимый человеку отказ имел код и дословный текст. Вводятся два кода по образцу уже существующего `CLASS_HAS_MARKS`, по одному на объект: **`STUDENT_HAS_MARKS`** («У ученика есть выставленные отметки — запись деактивируется, а не удаляется») и **`STAFF_HAS_HISTORY`** («У сотрудника есть привязки к предметам или выставленные отметки — карточка деактивируется, а не удаляется»). Кодов в версии становится **29**. Один общий код на оба объекта отвергнут: текст «у записи есть история» не называет объект, а правило §9 требует называть его. | [РЕШЕНО] | G-48, G-38 | L-9, L-15 |
| AR-115 | **Средний балл — колонка журнала, а не невидимое поле контракта.** `JournalDto.average` появился в контракте этапа 1, потому что 30-spec прямо говорит «средний балл считается чтением, подписчика в 1.1.1 нет», — но экранный реестр §10 не содержал элемента, который бы его показывал. Расхождение молчаливое и опасное именно этим: контракт возит на клиент число, которому некуда попасть, а правило «не выдумывать элементов» запрещает агенту нарисовать колонку по своей воле. Две честные развязки — убрать поле из DTO либо внести элемент в реестр; первая противоречит утверждению спеки о том, что средний балл существует и считается на чтении. Принято второе: в `S-50` вводится **`S-50.col.average`** — последняя колонка таблицы, только чтение, без действия и без отдельного права (видна всем, кто читает журнал). Правило расчёта не меняется и остаётся одно на систему: среднее по числовым 2–5, «н» и «б» не участвуют (AR-79); числовых нет — «—», а не ноль (свойство P7). | [РЕШЕНО] | G-38, G-52 | L-15 |

| AR-161 | **Именной QR: ФИО — атрибут карточки и подпись над кодом** (решение владельца 2026-08-28): карточку с ФИО заводит модератор, над QR печатается «Фамилия Имя», сканирует названный человек — **и сразу оказывается в кабинете, не вводя ничего**: формы активации не существует, экран `S-03` выводится из употребления (уточнение записи правкой владельца 2026-08-28). Заменяет порядок 1.1.1, где имя вводил сам регистрирующийся, а карточка до регистрации была 🔒 без имени. Самостоятельной регистрации по-прежнему нет (AR-95 в силе); зал ожидания и ссылки-инвайты трёх типов — отложенное с условием (`specs/school-launch/00-scope.md` §4). Рабочий экран события — фильтр «Не авторизованные» по всем типам карточек. **Уточнение 2026-08-30: рабочий экран события переезжает в раздел «Пользователи» (AR-169), фильтр «неавторизованные»; остальное — в силе.** | [РЕШЕНО] | G-66 | L-14, L-16 |
| AR-167 | **Лендинг v3: карусель школ + экран школы с тройным входом** (решение владельца 2026-08-30, У-12; вместе с AR-166 заменяет AR-163): школы переезжают вниз лендинга `S-00` лентой-каруселью — автопрокрутка слева направо, как логотипы партнёров; **до порога в 5 школ — статичный ряд без анимации** (в прод-базе одна школа — лента из одной карточки вырождается); `prefers-reduced-motion` уважается всегда. Клик по карточке → **экран школы** `/schools/:id` (`S-93`): имя/лого + три способа входа — 6-значный код (скоуплен этой школой, AR-166) · юзернейм+пароль (глобальный ключ AR-154 сохраняется, членство в этой школе проверяется, отказ — тот же `LOGIN_FAILED`) · QR привязки устройства. Нужен `GET /v1/schools/:id` — те же четыре поля, что у листинга AR-163, ничего сверх. Модалка `M-21` выводится (мёртвый код; её режим «код с корня» противоречит AR-166). Каталог `/schools` (`S-92`) остаётся фолбэк-маршрутом; `/login` остаётся прямым маршрутом для next-редиректов и памяток — оба [дефолт]. | [РЕШЕНО; фолбэк-маршруты — дефолт] | G-72 | L-14, L-16 |
| AR-169 | **КПЦ, раздел «Пользователи»: единый список учёток школы** (решение владельца 2026-08-30, У-1/У-5): все учётки (персонал + ученики + родители; единица — учётка с карточкой-носителем, записи контингента без учётки показываются пометкой «без доступа»), фильтры: **все · авторизованные · неавторизованные · удалённые** (в v1 «удалённые» = деактивированные, AR-168). Строка открывает модалку `M-22`: ID, ФИО, юзернейм, роли, статус и кнопки «QR» · «URL» (представления ссылки AR-165) · «6-значный код» (AR-166) · «Перевыпустить пароль». **Поле password текущий пароль не показывает — показ = перевыпуск с одноразовым показом** (AR-156 не трогается, хранение bcrypt-only). Поглощает блок `S-32` «Не авторизованные» (см. уточнение AR-161). | [РЕШЕНО] | G-73 | L-14, L-8 |
| AR-175 | **УТЦ: настройка расписания — одна полноэкранная модалка; результат — полноэкранные вкладки «Расписание | Настройка»; ленты дней и недель** (решения владельца 2026-08-30, №10 и целевое описание расписания). (1) Вместо пошагового мастера `M-08` — **одна большая модалка на весь экран**, где видны сразу все параметры (четверти · нагрузка · приоритеты · скелет дня AR-171); после сохранения и получения результата экран делится на **две вкладки во весь экран** — «Расписание» и «Настройка» (переключатели как вкладки браузера). (2) Расписание, мобайл: строка дней ПН…ВС; **дата — только под открытым днём**; горизонтальное плавное листание с фиксированной открытой позицией; раскрытие списка уроков сверху вниз; урок = «N. Предмет (1|2)» + «HH:MM — HH:MM» из скелета; обед и события скелета — строками в общей нумерации. (3) Расписание, десктоп: **лента недель** (мотается курсором, прецедент `S-50.weeks`), открывается вся неделя двумя колонками по три дня (ПН/ВТ/СР слева, ЧТ/ПТ/СБ справа). (4) Дневник рендерит **тот же компонент** (один код, не копия) + отметка после названия предмета; ДЗ — v2.0. Терминология: канон центра — **УТЦ** строго (решение №1; «УПЦ» — повторная опечатка, записи об этом в `20-cabinets.md` остаются в силе). | [РЕШЕНО] | G-75 | L-16, L-14 |
| AR-181 | **Журнал педагога — карточки СВОИХ позиций; журнал группы отдельный** (решение владельца 2026-08-31). Педагог входит в «Журнал» через карточки своих привязок: «Английский язык · 5 класс · группа 1»; у группового предмета — по карточке НА КАЖДУЮ группу, и открытый журнал группы несёт только её колонки и строки (`JournalColumnDto.groupNo`/`JournalRowDto.groupNo`; колонки `groupNo=0` — уроки всего класса — видны в обоих). Селекторы «класс × предмет» остаются модератору и завучу: у них привязок нет по роли, их объект — вся школа. Возврат — «‹ Мои журналы». Гейты отметок не меняются: карточки — витрина доступа, право же по-прежнему судит сервер (`journal.mark.post`, свой урок / модератор — AR-88). | [РЕШЕНО] | G-53 | L-8, L-16 |

| AR-184 | **Перезагрузка жестом и тихое самообновление** (правка владельца 2026-08-31: «на сайте нужна возможность перезагрузки и синхронных изменений»). (1) Вертикальная overscroll-цепочка мобильного скроллпорта (`.sch-shell--mobile .sch-content`) обязана доходить до вьюпорта — `overscroll-behavior-y: auto`, НЕ `contain`: иначе Chrome Android не показывает нативный pull-to-refresh. Парная обязанность: СЛОЙ целиком — `.sch-overlay` и полноэкранный `.sch-fullscreen` — ловушка overscroll (`overflow: hidden` + `overscroll-behavior: contain`; модалки рендерятся без портала, и без ловушки тяга по шапке/футеру/ручке листа уходила бы в скроллпорт), тело модалки и `.sch-studio-body` дополнительно держат `overscroll-behavior-y: contain` — тяга в открытой форме не должна перезагружать страницу с введённым. (2) `useAsync` перезагружает данные при возврате в приложение (`focus` / `visibilitychange`) ТИХО — без `status: "loading"` (скелетоны размонтировали бы открытые модалки), не чаще раза в 15 секунд, только из состояния `ready` и НИКОГДА при открытом слое (`.sch-overlay`/`.sch-popover`): часть экранов выводит сущность модалки из `state.data`. WebSocket/поллинг не вводятся (AR-87 остаётся только у карточек ожидания скана): «синхронные изменения» школы — это свежие данные при возврате к вкладке, а не живой стрим. | [РЕШЕНО] | G-53 | L-16, L-8 |
| AR-185 | **Релиз доезжает до устройства: версия воркера связана со сборкой, навигация — network-first** (дефект прода 2026-08-31: владелец не увидел задеплоенный пакет ни с телефона, ни с десктопа, и обновление страницы не помогало; деплой при этом был зелёным). Корень — `apps/web/public/sw.js` копировался в `dist` БЕЗ обработки и был байт-в-байт одинаков 68 коммитов подряд: алгоритм Update спецификации Service Worker сравнивает БАЙТЫ скрипта, одинаковые байты значат «обновления нет», поэтому `install`/`activate` не выполнялись ни разу с первой установки, а стратегия «сначала кеш» отдавала прошлую оболочку. Вводится: (1) воркер СОБИРАЕТСЯ из `apps/web/src/sw-source.js` плагином сборки, подставляющим идентификатор сборки (хеш входного чанка) — байты меняются каждый релиз; (2) навигация и `index.html` — network-first, запрос мимо HTTP-кеша (`cache: "reload"`), кеш остаётся ОФЛАЙН-фолбэком (AR-40 в части офлайн-чтения сохраняется, стратегия навигации из него вытесняется); ключ оболочки в кеше ОДИН (`/`) — раздельные записи по SPA-роутам протухали независимо, и роут, по которому никто не перезагружался, застревал навсегда; (3) `/assets/**` — cache-first в ОТДЕЛЬНОМ неверсионируемом кеше, `activate` чистит только оболочки прошлых сборок: чистка хешированных чанков ломает вкладку, открытую в момент деплоя (404 ленивого чанка сканера); (4) запись в кеш живёт в `event.waitUntil` и принимает только успешные ответы — иначе воркер усыпляется до записи, а 404 старого чанка ложится под именем скрипта и даёт белый экран; (5) `index.html`, `/sw.js`, `/manifest.webmanifest` отдаются с `Cache-Control: no-cache`, `/assets/` — одним заголовком `immutable`; (6) принудительный `location.reload()` при активации НЕ вводится: он противоречит AR-184 (потеря введённого в открытой форме), зацикливается на первом захвате `clients.claim` и не имеет выхода в PWA standalone — обновление приезжает следующей навигацией. Цена названа: залипшее устройство расклеивается со ВТОРОГО открытия, первое ещё исполняет прошлый бандл. | [РЕШЕНО] | G-80, G-53 | L-16, L-9 |
| AR-190 | **Система иконок — `lucide` через единственный компонент `Icon`** (размеры 18 рядом с текстом, 20 в навигации и кнопках-иконках, 24 в таб-баре; толщина линии 1.75); юникод-глифы в роли иконок запрещены и проверяются машинно (`tools/design/check-icons.mjs`, G-83). Вместе с этим калибруются кнопки и типографика: подъём кнопки на hover снят, движение остаётся только ответом на действие пользователя (reduced motion соблюдается по построению), шрифты Inter и Manrope загружаются на деле — токены ссылались на них с 1.1.1, но ни один `@font-face`/`<link>` их не подключал, и интерфейс отрисовывался системным шрифтом. Почему компонент, а не набор SVG-файлов и не глифы: одно имя на действие — второго рисунка у того же действия не бывает, а глиф из шрифта зависит от платформы и ломает выравнивание строки. Цена: имя иконки — часть контракта компонента; новую иконку добавляет владелец `icons.tsx`, а не любой экран. | [РЕШЕНО] | G-83 | L-16, L-8 |
| AR-191 | **Хаб настроек `S-82` и экран установки приложения `S-81`** с явными карточками Android и iPhone, а не подсказкой мелким шрифтом. `beforeinstallprompt` перехватывается при загрузке модуля — событие приходит раньше монтирования экрана и иначе теряется; Android получает системный диалог, iPhone — пошаговую инструкцию (Safari события установки не даёт); установленное состояние читается из `display-mode: standalone`. Меню пользователя `M-15` получает пункт «Настройки и приложение»; смена и удаление фото переезжают в `S-82`. Почему отдельный экран: вход из установленного приложения — единственный способ удержать сессию на телефоне без повторного входа (AR-183), а установка, спрятанная в подсказке, не выполняется. Цена: два экрана без единой мутации школы в реестре и в смоке. | [РЕШЕНО] | G-53 | L-16, L-14 |
| AR-192 | **Ячейки кода входа `S-05` — резиновые**: шесть ячеек делят ширину карточки, каждая квадратная от 40 до 56 px, фиксированной ширины ряда нет. Скриншот владельца 2026-09-02: на 390 px шесть ячеек фиксированной ширины выходили за карточку, и последняя обрезалась. Правило — ряд ячеек не шире карточки ни на одной ширине из `75-adaptive.md` §1; смок G-55 на 390×844 сверяет ячейки внутри карточки, а не только `body` по горизонтали — прежняя проверка `body` этот дефект не видела, потому что карточка резала переполнение сама. | [РЕШЕНО] | G-55 | L-16 |
| AR-197 | **Библиотека компонентов — Radix Primitives (headless) под собственными стилями на токенах; `@radix-ui/themes` и shadcn/ui отклонены** (пакет «Дизайн 1.4.0», шаг 1; решение по замерам 2026-09-03 — [library-choice.md](../design/1.4.0/library-choice.md), зонд `tools/design/lib-probe`). Слои `@radix-ui/react-dialog`, `react-popover`, `react-toast`, `react-tooltip` дают из коробки `role="dialog"`/`aria-modal`/`aria-labelledby`, ловушку и возврат фокуса (в том числе при удалении сфокусированного узла — дефект, который 1.3.0 чинила руками), `Esc`, закрытие по клику мимо, блокировку прокрутки и позиционирование с уходом от края; форма слоя — модалка, полноэкранный поток, нижний лист (`75-adaptive.md` §3) — остаётся нашим CSS на тех же `data-shape`. Почему не Themes: собственная палитра 12-ступенчатыми шкалами (`--violet-9` = `#6e56cf` против токена `violet-700` `#6427C9`), собственные шкалы шрифта и радиуса, 695 КБ CSS с 1555 hex-литералами — цвет перестаёт браться из `tokens.json`, и G-37 доказывает пары, которых на экране нет; `Select` рисует свой SVG-шеврон мимо `Icon` (G-83). Почему не shadcn: требует Tailwind — второй язык стилей рядом с `app.css` и экранами на `sch-*` (дублирование, которое AR-43 уже считает дефектом), а утилиты палитры (`text-red-600`, `bg-black/60`) кодируют цвет ни литералом, ни токеном — сканер G-37 их не видит. Замер (Vite 5.4, React 18.3, gzip, дельта к пустому React-приложению 46,6 КБ): Primitives Dialog+Popover+Toast+Tooltip **+32,3 КБ JS, CSS 0**; shadcn +41,1 КБ JS + тулчейн Tailwind; Themes +42,4 КБ JS **+81,9 КБ CSS**. Правила: (1) `@radix-ui/*` импортируется только в `ui.tsx` и `shell.tsx` — экран о библиотеке не знает; (2) `role="dialog"`, ловушка `Tab` и позиционирование слоя руками в контуре больше не пишутся; (3) `<select>` остаётся нативным — `react-select` стоит +29,3 КБ gzip ради выпадашки, которую телефон рисует системной; (4) `data-testid` и `data-shape` стоят на тех же узлах, что и в 1.3.0 (G-52, G-53, G-55). Цена: +~32 КБ gzip к бандлу; 15 транзитивных пакетов `@radix-ui/*` за одним `react-dialog`; обновление Radix — правка одного файла `ui.tsx`, а не экранов. | [РЕШЕНО] | G-84 | L-16, L-5, L-10 |

_Последняя ревизия блока: 2026-09-03 (Radix Primitives как библиотека слоёв — пакет «Дизайн 1.4.0», решение по замерам установкой и сборкой); ранее 2026-09-02 (система иконок и калибровка, настройки и установка приложения, резиновые ячейки кода — решения владельца «кабинеты 1.3.0»); ранее 2026-08-31 (доставка релиза на устройство: версия воркера и network-first — дефект прода; жест перезагрузки и тихое самообновление — правка владельца, вечерний раунд; ранее 2026-08-30 (полноэкранная настройка расписания, вкладки, ленты дней/недель — решения владельца «УТЦ v1.4»; лендинг v3 и раздел «Пользователи» — решения владельца по реестру узлов «Вход 1.3»; ранее 2026-08-28 (именной QR и экран «Не авторизованные» — запуск школы; ранее 2026-08-21: средний балл как колонка журнала, имена отказов при удалении записи с историей — этап 2; обратимость разрушающих операций — генеральный аудит перед кодом)._
