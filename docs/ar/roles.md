<!-- ar-register: id=roles; ranges=16-20,35,53,60-62,88-89,102,109,112,150-153,173-174,182,186,189,195-196,204-205,211 -->

# АР-реестр · Владение, роли, права

> Блок ведения: **Владение, роли, права**. Владелец: архитектура ядра. Диапазоны номеров: `16-20,35,53,60-62,88-89,102,109,112,150-153,173-174,182,186,189,195-196,204-205,211`.
> Нумерация сквозная по всей системе — см. [AR-PROTOCOL.md](../method/AR-PROTOCOL.md).
> Индекс всех реестров: [INDEX.md](./INDEX.md). Ворота: [G-CHECKS.md](../G-CHECKS.md). Линзы: [LENSES.md](../method/LENSES.md).

| № | Решение | Статус | Ворота | Линзы |
|---|---|---|---|---|
| AR-16 | Две оси ролей: **DOMAIN** (teacher/student/parent/staff — в `Membership`, приходят в токене) и **TENANCY** (operator/org_admin/workspace_admin — у Флёра, в токен НЕ приходят). Каталог прав строится только на доменных; кабинет owner — панель Флёра. **Уточнение AR-35 (2026-07-28): `admin` из `florus_orgs[].role` — организационная роль, ПРИХОДИТ в токен и провижинится в Membership → получает пакет в каталоге (структура школы, устройства, парсер); не путать с tenancy-ролями панели.** | [заменено AR-53] | G-10 | L-8 |
| AR-17 | `staff` маппится в под-роль `{завуч, методист, психолог}` **локально**; назначает школьный админ; приоритет: назначенная ранее → hint из инвайта → `methodist`. | [заменено AR-60] | нет: дефолт (под-роли) | L-3, L-8 |
| AR-18 | Инвариант **«система предлагает — человек решает»**: Solver→утверждает завуч; летучка→оценку ставит учитель; сдвиг КТП (`ktp.shift.proposed`)→ждёт человека; `suggest-mode`→advisory. Никакой авто-апплай ИИ-результата не допускается ни в одном будущем модуле. | [РЕШЕНО] | G-3 | L-8, L-1 |
| AR-19 | Разделение обязанностей enforced через RBAC: `planning.*.approve` — только завуч; `lesson.conduct` — только учитель. Учитель не утвердит, завуч не проведёт. | [РЕШЕНО] | G-10 | L-8 |
| AR-20 | ИИ-точки — **детерминированные стабы с помеченной точкой замены** (parseTextExtract, Tesseract-стаб летучки, suggest-mode). Замена на реальную модель не меняет контракты событий и таблицы. | [РЕШЕНО] | G-5 | L-6, L-9 |
| AR-35 | **Покрытие RBAC**: `@RequirePermission` стоит на 27 из ~120 роутов; мутации `structure`, `doc`, Phase-0 `journal`, `consent`, `contingent`, `comm`(edit/react) не гейчены. Дефолт: каждая мутация обязана иметь гейт; каталог прав пополняется в том же коммите. | [дефолт] | G-10 | L-8, L-15 |
| AR-53 | **Tenancy-роли живут в Schoolium**: панель владельца школы и владельца сети — экран Schoolium, а не внешней платформы. Разделение осей сохраняется (DOMAIN — teacher/student/parent/staff в `Membership`; TENANCY — владелец workspace/worknet), но носителем tenancy-оси становится сам продукт. Права владельца сети ограничены перечислением школ сети (AR-44), а не обходом изоляции. Заменяет AR-16. | [РЕШЕНО] | G-10, G-22 | L-8, L-7 |
| AR-60 | **Ролевая модель v1 — шесть ролей**: учредитель, директор, заместитель по воспитательной работе, заместитель по учебной работе, преподаватель, модератор школы. Ученик и родитель в v1 отсутствуют как аккаунты (появляются в 1.1.3, AR-71). Роли **совместимы**: один человек может нести несколько ролей в одной школе. Заменяет прежнюю раскладку под-ролей `staff` (AR-17). | [заменено AR-150] | G-10 | L-3, L-8 |
| AR-61 | **Совместимость ролей не ослабляет разделение обязанностей**: инвариант «утверждающий ≠ автор» проверяется по **идентичности человека**, а не по роли. Преподаватель, одновременно являющийся заместителем по учебной работе, не утверждает собственный КТП — утверждает другой носитель права. В малой школе это норма, а не крайний случай, поэтому правило вводится сразу, а не после инцидента. | [РЕШЕНО] | G-27 | L-8, L-1 |
| AR-62 | **Модератор школы — оператор жизненного цикла пользователей** (добавление, смена телефона, деактивация), но **не завершает операцию над чужим аккаунтом в одиночку**: инициирует модератор, подтверждает владелец аккаунта (QR или код) либо директор/учредитель. Иначе модератор — прямой путь захвата аккаунта директора. Все действия модератора попадают в аудит (AR-30). | [заменено AR-88] | G-28 | L-8, L-13 |
| AR-88 | **Модератор школы — полные права внутри своей школы** *(отменено AR-148 решением владельца 2026-08-27: полнота прав переезжает к администратору школы, у модератора остаются мелкие роли — настройка расписания и подобные. Раздел каталога прав между двумя ролями — работа блока ролей.)* (решение владельца 2026-08-21): любая мутация версии без исключений — ведение классов и контингента, предметы, персонал, расписание, **постановка и снятие отметок в любом уроке**, удаление и деактивация пользователей. Совмещение ролей модератору не нужно: полномочия и так полные, поэтому в малой школе он ведёт уроки и заполняет журнал под своей единственной ролью. Заменяет двухшаговость AR-62. **Названная цена:** модератор — единственная точка, из которой достижим любой аккаунт школы, включая директора и учредителя; сдерживающий механизм остаётся один — полный аудит действий модератора (AR-30), поэтому аудит перестаёт быть фоновой функцией и получает собственные ворота. **Полные права не отменяют гейтов реальности**: отметка в непроведённый урок отклоняется и модератору (`LESSON_NOT_HELD`) — это факт календаря, а не уровень доступа (AR-74). | [заменено AR-148] | G-41 | L-8, L-13 |
| AR-89 | **Удаление и деактивация сотрудников входят в 1.1.1** (решение владельца 2026-08-21, следствие AR-88). Правило подмены то же, что у ученика (AR-78): сотрудник без привязок и без выставленных отметок **удаляется**, сотрудник с историей — **деактивируется**, и деактивация обратима реактивацией. Каскад удаления и деактивации: привязки к предметам снимаются (`subject.teacher.unbound.v1`), покрытие предметов падает, сетка помечается `stale` — уроки без исполнителя видны человеку, а не тихо исчезают; выставленные им отметки остаются, `postedBy` — историческая ссылка, а не живая связь. **Защита школы:** последний активный модератор не удаляется и не деактивируется (`LAST_MODERATOR`) — иначе школа остаётся без единственной роли, которая ей управляет; второй модератор удаляется свободно, правило защищает школу, а не должность. | [РЕШЕНО] | G-43 | L-8, L-9 |
| AR-102 | **Второй модератор заводится выдачей роли, а не отдельным разделом**: кнопка «Добавить роль» на карточке зарегистрированного сотрудника (`S-31`) выдаёт `moderator` любому — и той же кнопкой роль снимается. Отдельной секции «Модераторы» на экране персонала нет: модератор — не должность в штатном расписании школы, а уровень доступа. Два предохранителя: роль последнего активного модератора не снимается (`LAST_MODERATOR`, тот же отказ, что у удаления — AR-89), и последняя роль сотрудника не снимается вовсе (`LAST_ROLE`) — для закрытия доступа существует деактивация, а сотрудник без единой роли — это запись, о правах которой невозможно рассуждать. Первый модератор школы по-прежнему приходит только из bootstrap (AR-93). | [РЕШЕНО] | G-43 | L-8, L-3 |

| AR-109 | **Второй модератор — штатная фигура (AR-102), поэтому одновременная правка обязана иметь названный исход.** Стенд однопоточен и честно вынес конкурентность в слепые зоны, но вынес их **до** того, как AR-102 сделал двух одновременных модераторов нормой, — и с тех пор к вопросу никто не возвращался. Общей блокировки версия не вводит: она стоит дороже пилота одной школы. Вводится **условная запись по версии агрегата** ровно на трёх операциях, где расхождение стоит данных или пересборки: подтверждение сетки (`POST /schedule/confirm`), правка нагрузки и параметров дня (`PUT /schedule/load`, `/day-params`) и массовое создание классов (`POST /classes/bulk`). Мутация несёт версию прочитанного состояния; расхождение — отказ `CONCURRENT_EDIT` с именем второго модератора и предложением перечитать экран. Остальные мутации версии идемпотентны либо адресны (правка одного профиля, одна отметка, одна роль) — там последняя запись побеждает, и это записанный выбор, а не умолчание. Скан QR в момент закрытия карточки закрыт тем же правилом одноразовости токена, что и повторный скан: гасит токен первая успешная операция, вторая получает `TOKEN_USED`. | [РЕШЕНО; общая блокировка — слот] | G-51 | L-4, L-9 |

| AR-112 | **Отметка собственного скана — identity-gated операция и в таблицу мутаций школы не входит.** Карточка `S-22` узнаёт о скане поллингом (AR-87), но кто-то обязан этот скан зафиксировать: педагог наводит камеру из `S-70`, и токен переходит в `scanned`. В §11 строки для этого действия не было, а прямая выдача ему права из каталога ломает утверждение G-41 «четыре читающие роли не проходят НИ ОДНОЙ мутации»: педагог с `subjects.read` внезапно оказывается автором мутации предметов. Решение: `POST /api/v1/subjects/scan` гейтится **владением**, а не кодом каталога — ровно как выход из сессии и адресное завершение чужой сессии из `S-80`: человек отмечает СВОЙ скан своей сессией. В воротах G-10 у неё строка whitelist с причиной, в §11 — абзац рядом с поллинговыми чтениями. Мутаций школы по-прежнему 38, и «действия вне таблицы не существует» продолжает читаться буквально: таблица перечисляет мутации ШКОЛЫ, а не всё, что меняет строку в базе. | [РЕШЕНО] | G-10, G-41 | L-8, L-15 |

| AR-150 | **Ролевая модель v2 — восемь ролей** (решение владельца 2026-08-28; уточнение записи правкой владельца в тот же день): к шести ролям v1 добавляются `admin` (администратор школы — полное управление workspace, реализует разведение AR-148), `parent` и `student` (аккаунты — AR-155). Модератор остаётся ролью рутинной настройки. Слова словаря владельца (`hostess`, `methodist` и др.) — **имена для сущностей, а не задание на реализацию**: ролями версии не являются, имена зарезервированы. Роли по-прежнему совместимы (массив). Заменяет AR-60. Глобальный админ EduStore/Schoolium — не роль школы, а консольный контур AR-93. | [РЕШЕНО] | G-66 | L-3, L-8 |
| AR-151 | **Видимость кабинетов — матрица владельца 2026-08-28** (`specs/school-launch/20-cabinets.md` §2), принцип AR-69 сохраняется: экраны общие, различие — данные и действия. Совмещения «+ профили своих детей» и «+ журнал своих предметов» выводятся из фактов (`GuardianLink`, `TeacherBinding`), а не из ролей — иначе каждый совместитель требовал бы отдельной роли, а разделение обязанностей по идентичности (AR-61) размывалось бы. | [РЕШЕНО] | G-66 | L-15, L-8 |
| AR-152 | **Панель УТЦ — у завуча, КПЦ/УПЦ — у модератора, все мутации — у администратора школы** (матрица владельца 2026-08-28; уточняет AR-148, чья редакция отдавала настройку расписания модератору). Раскладка прав по ролям — таблица `specs/school-launch/20-cabinets.md` §2; отнесение предметов к панели УТЦ — [дефолт] внутри неё. Закрывает хвост AR-148 «раздел каталога прав — работа блока ролей». | [РЕШЕНО] | G-66 | L-8, L-3 |
| AR-153 | **Отзыв активации** — операция «просканировал не тот» (решение владельца 2026-08-28): доступна `admin`/`moderator`, закрывает все сессии учётки, возвращает карточку в состояние «не авторизован», перевыпускает токен; история карточки не затрагивается. **Уточнение 2026-08-30 (дефект текста, П-5): креды НЕ обнуляются** — их заводил модератор, чужой сканер их не знает; так зафиксировано принятой спекой `specs/school-launch/10-identity.md` §«Отзыв» и так работает код (`staff.service.ts`). Прежняя формулировка «обнуляет юзернейм и пароль» противоречила обоим источникам и выправлена по ним. Дополняет одноразовость токена AR-109 обратным ходом, которого в 1.1.1 не было (там ошибочный скан лечился удалением карточки). Строка аудита обязательна (расширение G-41). | [РЕШЕНО] | G-65 | L-9, L-14 |
| AR-173 | **Удаление пользователя = архивация на 10 дней, затем полное удаление** (решение владельца 2026-08-30, №3; замещает пару «AR-89/AR-78: с историей — только деактивация навсегда» и уточняет AR-168): удаление любой учётки/записи с историей переводит её в архив (`archivedAt`), где она живёт **10 дней** и видна в фильтре «Удалённые» (КПЦ) с действием «Восстановить»; по истечении — **полное физическое удаление** фоновой задачей. Запись без истории удаляется сразу, как раньше. Юридическая сторона названа: 10-дневный архив — это и есть срок хранения ПДн после «удаления»; вычистка по таймеру обязана быть исполняемой (ворота). | [РЕШЕНО] | G-77 | L-9, L-1 |
| AR-174 | **Завуч в технических настройках УТЦ не участвует** (решение владельца 2026-08-30, №9; уточняет матрицу `20-cabinets.md` §2 и AR-152): `deputy_academic` расставляет **только годовые нормы часов по предмету** (нагрузка, шаг «Нагрузка» настройки расписания) — ни скелета дня, ни генерации, ни календаря, ни привязок педагогов. Панель УТЦ целиком — `admin` + `moderator`; каталог прав выравнивается: `schedule.build`/`subject.write` переезжают модератору, у завуча остаётся право нагрузки (новый код `schedule.load.write`). | [РЕШЕНО] | G-66 (расширение) | L-8, L-3 |

| AR-182 | **Замы добавляются с экрана персонала; единственность синглтонов держит сервер** (правка владельца 2026-08-31: «заместителей добавить нельзя — только дирекция и учителя. исправь»). `STAFF_SECTIONS.addable` становится массивом ролей: секция 2 несёт кнопки `S-30.btn.addDeputyAcademic` / `S-30.btn.addDeputyUpbringing`. Прежний безусловный запрет `SINGLETON_ROLES` в `addCard` — наследие эпохи AR-60 («единичные позиции существуют слотами из bootstrap») — оставлял школу вовсе без завуча: bootstrap 1.2.0 слотов директора и замов не создаёт. Смысл `SINGLETON_ROLES` меняется с «не создаётся» на «не более одного носителя»: роль занята, если её несёт живое членство (`deactivatedAt: null`) ЛИБО незаполненный слот в `plannedRoles`; занятая — отказ и в `addCard`, и в `addRole` (`M-07` иначе позволял второго завуча, и единственность оставалась декларацией, П-4). Деактивированный носитель роль освобождает (AR-89). Директор собственного канала заведения не имеет: кнопки нет, и bootstrap его карточку тоже НЕ создаёт — директором становится существующий сотрудник выдачей роли через `M-07` (пара «учредитель+директор»), и единственность держит тот же серверный синглтон; проверка стоит и на реактивации — путь «деактивировать → завести нового → реактивировать старого» второго живого носителя не даёт. | [РЕШЕНО] | G-43, G-53 | L-3, L-8 |
| AR-186 | **Три кабинета по праву: администратор `/admin` (`S-62`, `school.admin`), модератор `/moderator` (`S-60`, `school.manage`), завуч `/deputy` (`S-61`, `school.oversee`)** (решение владельца 2026-09-02). Что администрируется где: **администратор** — сеть и устройства всех людей школы (карта устройств, журнал подключений, дистанционная активация ссылкой 48 ч, отзыв любой сессии, инцидент-режим), политика лимитов сессий, матрица ролей и прав (чтение), полный аудит школы по всем действующим, реестры Wi-Fi и корпоративных устройств, ссылка входа с карточки сотрудника; **модератор** — КПЦ (классы, ученики, родители, карточки персонала, активации, коды входа, перевыпуск пароля) и панель УТЦ (предметы, привязки, сборка расписания); **завуч** — чек-листы готовности УТЦ и КПЦ (чтение), нормы часов (AR-174 не меняется), журналы школы на чтение. Почему по праву, а не по роли: `school.admin` держит только `admin`, `school.oversee` — завуч и администратор, поэтому человек с двумя ролями видит оба кабинета без второй учётки, а закрытый кабинет отвечает 403 с причиной (AR-69). Маршрут `/admin` переходит от кабинета модератора к кабинету администратора; кабинет модератора живёт на `/moderator` — закладка прежнего адреса у модератора ведёт в 403 с причиной, не в пустую страницу. Цена названа: администратор становится единственной ролью, способной завершить любую сессию школы; противовес — каждая мутация кабинета аудируется с идентичностью администратора и доказывается перечислением (G-81). Операционная матрица — `docs/CABINETS.md`, единственное место (П-5). | [РЕШЕНО] | G-81 | L-8, L-3 |
| AR-189 | **Одноразовая ссылка входа живёт 48 часов** — и консольная (`school:bootstrap`; `school:provision` для администратора, модератора и завуча), и выданная администратором с карточки сотрудника (`S-31`/`S-62`, `POST /api/v1/staff/:id/login-link`, право `school.admin`). Решение владельца 2026-09-02: было 24 ч — оператор, получивший ссылку вечером накануне события, не успевал ею воспользоваться, и школа теряла день на перевыпуск. Обе ссылки — одна таблица `BootstrapLink` с полями `purpose` (`bootstrap` | `login_link`) и `issuedBy` (NULL у консольной): второй таблицы для того же токена не заводится (П-5). Погашение ссылки активирует членство (AR-161) и открывает стартовый экран роли; повторное открытие — `TOKEN_USED`. Дистанционная активация с карточки — только администратору: модератор выдаёт вход именным QR и кодом на месте, а ссылку, уходящую в мессенджер, выпускает тот, чьи действия аудируются полностью. Цена: 48 часов — вдвое длиннее окно, в котором перехваченная ссылка действует; противовес — одноразовость и `issuedBy` в аудите (G-44 переписана под 48 ч). Одноразовость снята AR-195 (2026-09-03). | [заменено AR-204] | G-81, G-44 | L-8, L-14 |
| AR-195 | **Ссылка входа многоразовая до истечения 48 часов** (решение владельца 2026-09-03: одноразовая ссылка сгорала на первом устройстве, и человек, вошедший с телефона, не мог открыть кабинет с ноутбука). Каждое открытие в срок выдаёт новую сессию (`useBootstrapLink` не отклоняет по `usedAt`); `usedAt` хранит первое открытие для аудита; после `expiresAt` — `TOKEN_EXPIRED`. Цена: перехваченная ссылка действует до конца срока; противовес — 48 часов, отзыв сессий администратором (AR-188) и `issuedBy` в аудите. Именной QR активации и код входа остаются одноразовыми. | [заменено AR-204] | G-44, G-81 | L-8, L-14 |
| AR-196 | **Годовые нормы часов выставляют только завуч и администратор** (решение владельца 2026-09-03): `PUT /api/v1/schedule/load` гейчен только `schedule.load.write`, кнопка `S-40.btn.load` показывается только держателю этого права; модератор (`schedule.build`) норм не видит и не меняет — панель УТЦ у него без норм. Уточняет AR-174: «завуч расставляет нормы» теперь означает «и только он, кроме администратора». Цена: школа без завуча выставляет нормы администратором. | [РЕШЕНО] | G-41 | L-8, L-3 |
| AR-204 | **Ссылка входа выпускается модератором и администратором с параметрами: срок 24/48 ч или 7 дней, число открытий 1/3/10 или без лимита** (владелец 2026-09-03: карточка сотрудника с «меню настройки по времени действия / количеству допустимых раз использования»; кнопки видят «админ и модератор»). `POST /api/v1/staff/:id/login-link` — право `staff.manage` (было `school.admin`, AR-189), тело `{ttlHours, maxUses}`, дефолты 48 ч и без лимита сохраняют поведение AR-195; `BootstrapLink.maxUses/useCount`: открытие в срок выдаёт сессию и увеличивает счётчик, исчерпание — `LINK_EXHAUSTED` (410, «использована N из M»). Консольные `bootstrap`/`provision` — без изменений (48 ч, без лимита). Событие `staff.login_link.issued.v1` несёт срок и лимит. Вытесняет AR-189 и AR-195 (их параметры стали дефолтами настраиваемых). | [РЕШЕНО] | G-81, G-87 | L-8, L-13 |
| AR-205 | **Единственность ролей — не константа, а лимит носителей роли в политике школы** (владелец 2026-09-03: «в „Политиках“ рядом с лимитом сессий сделай лимит ролей для школы: 1 завуч / директор и т. д.»). `SchoolAccessPolicy.roleLimits` (роль → число либо `null` «без лимита»), дефолты в коде `DEFAULT_ROLE_LIMITS = {director: 1, deputy_academic: 1, deputy_upbringing: 1}` — пустая политика ведёт себя как прежние синглтоны; носители = живые членства + пустые слоты `StaffCard.plannedRoles`; проверка в `addCard`, `addRole`, `reactivate` (`fillCard` — нет: слот уже носитель), отказ `ROLE_LIMIT_REACHED` (409, роль словами, «N из M»); меняет администратор в `S-62` «Политики» (`school.admin`), только для штатных ролей, потолок 20. Платформенный `school:provision` лимит не блокирует, а предупреждает [дефолт]. Уточняет AR-182: кнопки замов остаются, `SINGLETON_ROLES` — производная от дефолтов. | [РЕШЕНО] | G-43, G-81 | L-8, L-3 |
| AR-211 | **Учётной записью администратора школы управляет только администратор** (адверсарное ревью пакета 1.5.0). AR-204 отдал выпуск ссылки входа `staff.manage`, а «задать пароль» (AR-203) появился рядом с перевыпуском — и модератор получил способ в один клик выдать себе доступ администратора: задать ему пароль либо выпустить ссылку и открыть её. Разделение кабинетов (AR-186) держалось бы на том, что модератор так не поступит. Поэтому четыре операции контура доступа — `POST /staff/:id/password`, `POST /staff/:id/credentials`, `POST /staff/:id/login-link`, `PUT /staff/:id/account` — на карточке носителя роли `admin` требуют `school.admin`; отказ именной: `ADMIN_ACCOUNT_LOCKED` называет владельца карточки. Остальной персонал модератор ведёт как прежде (AR-88), включая роли, деактивацию и удаление: ограничение стоит там, где выдаётся ДОСТУП, а не там, где ведётся кадровый учёт. | [РЕШЕНО] | G-87 | L-5, L-10 |

_Последняя ревизия блока: 2026-09-04 (учётка администратора закрыта от модератора — адверсарное ревью пакета 1.5.0; пакет 04.09: ссылка входа с параметрами и правом модератора, лимиты носителей ролей в политике); ранее 2026-09-02 (три кабинета по праву, ссылка входа 48 ч — решения владельца «кабинеты 1.3.0»); ранее 2026-08-31 (замы добавляются, синглтон держит сервер — правка владельца, вечерний раунд; ранее 2026-08-30 (архивация удаления, границы завуча — решения владельца «УТЦ v1.4»); ранее 2026-08-28 (ролевая модель v2, матрица видимости, отзыв активации — решения владельца перед запуском школы; ранее 2026-08-21: отметка скана как identity-gated операция — этап 1; исход одновременной правки — генеральный аудит перед кодом)._
