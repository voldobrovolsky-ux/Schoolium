# Индекс АР-реестров

> Реестры разделены по **блокам ведения**, нумерация — **сквозная по всей
> системе**: номер уникален глобально, в каком бы файле он ни лежал.
> Правила — [AR-PROTOCOL.md](../method/AR-PROTOCOL.md). Проверяется машинно:
> `node tools/method/lint-ar.mjs`.

<!-- ar-index: next=163 -->

**Следующий свободный номер — значение `next` в машинной шапке выше (163).**
Новый блок ведения получает диапазон из 20 номеров начиная с него.

| Реестр | Блок ведения | Владелец | Диапазоны | Решений |
|---|---|---|---|---|
| [truth.md](./truth.md) | Источники истины и изоляция | архитектура ядра | 1-4, 6-7, 34, 44-48, 66, 78, 83, 98-99, 104, 106, 110, 154 | 21 |
| [platform.md](./platform.md) | Стек и рантайм | архитектура ядра | 8-15, 36-37, 49-51, 63-65, 76, 86-87, 91-94, 96-97, 117, 156-157, 162 | 29 |
| [events.md](./events.md) | Событийная архитектура | архитектура ядра | 5, 21-25, 52, 108 | 8 |
| [roles.md](./roles.md) | Владение, роли, права | архитектура ядра | 16-20, 35, 53, 60-62, 88-89, 102, 109, 112, 150-153 | 19 |
| [law.md](./law.md) | Право и безопасность детей | владелец продукта + юрист | 26-30, 70, 116, 155 | 8 |
| [product.md](./product.md) | Продукт, рынок, запуск | владелец продукта | 31-33, 54-59, 71 | 10 |
| [domain.md](./domain.md) | Учебный процесс и движок | предметная область | 38-39, 67-68, 72-75, 79, 84-85, 100-101, 103, 107, 111, 114, 118-149, 158-160 | 52 |
| [ui.md](./ui.md) | Интерфейс и клиент | фронтенд | 40-43, 69, 77, 80-82, 90, 95, 105, 113, 115, 161 | 15 |

**Статусы:** `[РЕШЕНО]` — зафиксировано и подтверждено · `[дефолт]` — разумный
выбор по умолчанию, изменяем решением владельца · `[узел]` — требует внешней
информации, в спеке НЕ считается закрытым · `[заменено AR-N]` · `[отменено]`.

## История

- **2026-07-28** — реестр извлечён из кода и `docs/` по состоянию `main`
  (`cfe3a09`, включая PR #10 «учебник → КТП → КПП»); владелец принял решения по
  всем внутренним узлам (AR-4, 23, 24, 29, 30, 32, 34, 38, 40 → `[РЕШЕНО]`).
  Открытым остался внешний **AR-33** (сторона Флёруса: регистрация клиента +
  секрет) — спеку он не блокирует.
- **2026-07-28** — реализованы и закрыты зелёными G-проверками: AR-34, AR-35,
  AR-23+24, AR-4, AR-29+30, AR-38 (сетка), AR-40. AR-36 — ядро реализовано,
  остаток G-14. AR-32 — кодовых изменений не требовал. Не реализовано (ждёт
  внешнего): AR-33 → G-18.
- **2026-08-19** — единый реестр разделён на блоки ведения при сохранении
  сквозной нумерации; добавлены колонки **Ворота** (G-проверка) и **Линзы**
  (что стережёт решение); заведена машинная проверка `lint-ar.mjs`.
- **2026-08-19** — принята окончательная продуктовая логика
  ([PRODUCT-LOGIC.md](../PRODUCT-LOGIC.md)): EduStore — маркетплейс сервисов,
  Schoolium — первый сервис (ERP школы). Приняты **AR-44…AR-59**; вытеснены
  решения внешнего IdP и производные от него: AR-1 → AR-46, AR-3 → AR-48,
  AR-14/AR-15 → AR-49, AR-16 → AR-53, AR-33 → AR-57. Разбор конфликтов —
  [specs/context-edustore/20-fit.md](../../specs/context-edustore/20-fit.md).
- **2026-08-20** — владелец закрыл оба открытых пункта: **AR-47** подтверждён
  (`[дефолт]` → `[РЕШЕНО]`), **AR-59** — Communitoria признана отдельным
  сервисом-мессенджером EduStore и выведена из объёма Schoolium (`[узел]` →
  `[РЕШЕНО]`). Зафиксирован объём v1: **AR-60…AR-71** — шесть ролей, кабинет
  модератора, QR как штатный механизм подтверждений, двухшаговая смена телефона,
  календарь учебного года как сервис-владелец, план версий v1 → 1.1 → 1.1.3.
  Вытеснено: AR-17 → AR-60. Сняты ворота G-6 (пилотный контур потерял предмет).
  **Открытых `[узел]` в реестрах нет.**
- **2026-08-20** — спека онбординга 1.1.1 (`specs/school-onboarding/`): приняты
  **AR-72…AR-78** (FSM мастера, шаблон недели + материализация, производный
  журнал, группы, присутственная QR-регистрация, паттерн карточек, деактивация
  вместо удаления); уточнена нумерация релизов в AR-71 (1.1.1 → 1.1.2 → 1.1.3);
  заведены ворота G-33…G-36.
- **2026-08-20** — владелец закрыл В1 (шкала отметок: 2–5, «н», «б» — AR-79) и
  В2 (жёсткая четвёрка, триместры — слот); принято дизайн-направление AR-80
  (фиолетово-розовая гамма, токены с исполняемой проверкой контраста G-37).
- **2026-08-21** — адаптивная спецификация: **AR-81** (оболочка: сайдбар+топбар
  на десктопе, хедер+таб-бар из пяти вкладок на мобайле, одинаковых для всех
  ролей) и **AR-82** (правила модалок: два уровня вложенности, полноэкранные
  мастера и нижние листы на мобайле). Ворота G-39.

## Компаньоны

- [G-CHECKS.md](../G-CHECKS.md) — исполняемые доказательства инвариантов (ворота).
- [FSM-MATRIX.md](../FSM-MATRIX.md) — матрицы полноты состояний.
- [LENSES.md](../method/LENSES.md) — реестр линз.
- [adr/](../adr/) — развёрнутые ADR по ключевым решениям (контекст, альтернативы, последствия).
- **2026-08-21** — этап 0 (генеральный аудит спеки 1.1.1 перед кодом, отчёт
  [81-audit-stage0.md](../../specs/school-onboarding/81-audit-stage0.md)): приняты
  **AR-83…AR-87** — канон отметок 1.1.1 (`Mark`), авторинг сетки генератором,
  жизненный цикл сетки после подтверждения, параметры контура доступа, ожидание
  скана поллингом. Вытеснено: AR-4 → AR-83, AR-38 → AR-84. Заведены ворота G-42 (номер выдан после слияния с веткой инструкций агентов, занявшей G-40);
  проверка G-38 усилена обратной сверкой кодов ошибок.
- **2026-08-21** — владелец закрыл вопрос В3 этапа 0: **AR-88** — модератор школы
  обладает полными правами внутри своей школы, от постановки отметок в любом уроке
  до удаления пользователей. Вытеснено: AR-62 → AR-88 (двухшаговость операций над
  чужим аккаунтом снята), ворота G-28 сняты вместе с предметом; противовесом
  остаётся аудит — заведены ворота G-41.
- **2026-08-21** — аудит обратимости (`81-audit-stage0.md`, раздел «Аудит удаления
  и отмены»): приняты **AR-89** (удаление и деактивация сотрудников в объёме 1.1.1
  с защитой последнего модератора) и **AR-90** (у каждой операции есть обратная
  либо названная причина). Вопрос В4 закрыт владельцем: двухшаговая смена
  телефона (AR-64) остаётся единственным исключением из AR-88. Заведены ворота
  G-43; проверка протокола реестров дополнена контролем уникальности номера ворот
  (после слияния в репозитории оказалось два разных G-40).
- **2026-08-21** — аудит маршрута входа (вопрос владельца «как сотрудник зайдёт
  в устройство»): приняты **AR-91** (регистрация по QR выдаёт сессию на
  устройстве сотрудника), **AR-92** (одноразовый код входа как обязательный
  резервный канал; деактивация отзывает сессии) и **AR-93** (первый модератор
  школы заводится платформенной операцией с одноразовой ссылкой). Заведены
  ворота **G-44**.
- **2026-08-21** — решения владельца по пилотному контуру: **AR-94** — вход без
  SMS вовсе (сессия 90 дней из регистрации, привязка устройств по QR по паттерну
  Telegram, код с карточки как восстановление; вытеснены AR-49 и AR-86, усиление
  безопасности — слот ~1.4); **AR-95** — сайт с лендингом и кнопкой «Вход», без
  самостоятельной регистрации; **AR-96** — БД пилота: Postgres в docker на том же
  VPS, ежедневный pg_dump (не SQLite, не облако); **AR-97** — именованные
  env-флаги отладки, маскирование ПДн не отключается; **AR-98** — тенант пилота
  настоящий: bootstrap создаёт реальный Workspace, дефолтной школы в коде нет.
  Экран S-02 (SMS-код) исключён; добавлены S-00 (лендинг) и S-80 (устройства).
- **2026-08-21** — закрытие блокеров перед кодом (сценарный проход спеки):
  **AR-99** — таблицы контура доступа названы поимённо и разведены с доменными,
  состояние привязки устройства живёт в БД; **AR-100** — справочник нерабочих
  дней по годам, отсутствие года = `CALENDAR_YEAR_MISSING`; **AR-101** —
  материализация идемпотентна и имеет три названных триггера; **AR-102** —
  второй модератор заводится выдачей роли, с защитой `LAST_MODERATOR` и
  `LAST_ROLE`. Заведены ворота **G-45**.
- **2026-08-21** — независимый генеральный аудит перед первой строкой кода
  (отчёт [81-audit-stage0.md](../../specs/school-onboarding/81-audit-stage0.md),
  раздел «Генеральный аудит перед кодом»): приняты **AR-103…AR-109**. Дневная
  сетка получила источник (AR-103) и бюджет перебора (AR-107); стык с физической
  схемой описан поимённо — три занятых доменных имени и три «переиспользуемые»
  таблицы без нужных полей (AR-104); разрушающие операции внесены в реестр
  обратимости, необратимых стало пять (AR-105); регистрация присоединяет
  существующего человека ко второй школе, не отказывая ему (AR-106); у события
  появились обязательные колонки «подписчик» и «реакция», заведено
  `contingent.student.deleted.v1` — событий 22 (AR-108); одновременная правка
  двумя модераторами получила названный исход `CONCURRENT_EDIT` (AR-109).
  Заведены ворота **G-46…G-51**; свойства модели выросли до **P1…P20**, детектор
  линзы L-9 расширен разрушающими операциями. Кодов ошибок 27, экранов 23,
  мутаций 38.
- **2026-08-21** — **этап 1 (контракты и каркас)**: реализованы схема данных,
  DTO, 22 события, каталог прав, генератор, жизненный цикл сетки, контур доступа
  без SMS и производный журнал. Шестнадцать ворот версии заведены и зелёные
  (G-24, G-33…G-36, G-41…G-51). Приняты **AR-110** (журнал хранит собственные
  проекции строк и колонок, внешних FK у него нет), **AR-111** (отказы, зависящие
  от «слотов недели», принадлежат экрану 4, а не экрану 2) и **AR-112** (отметка
  собственного скана — identity-gated операция вне таблицы мутаций школы).
  Событие `subject.deleted.v1` переименовано в `subject.card.deleted.v1`: три
  сегмента нарушали канон AR-23, и приоритет решения над спекой закрыл вопрос.
- **2026-08-21** — **этап 2 (десктоп)**: два вопроса, открытых этапом 1, закрыты
  решениями. **AR-113** — у отказа «удаление записи с историей» появились имена
  `STUDENT_HAS_MARKS` и `STAFF_HAS_HISTORY` по образцу `CLASS_HAS_MARKS`; кодов
  версии стало 29. **AR-114** — «уроков в день» это верхняя граница школьного
  дня, а дневной потолок СанПиН применяется к каждой параллели отдельно: иначе
  школа с первым и восьмым классом не собирается в принципе. Заведены ворота
  **G-52** (элементы экранов в DOM перечислением) и **G-53** (десктопный смок
  онбординга со скриншотами каждого шага).
- **2026-08-27** — **блок «Расписание» УТЦ**: слой генерации формализован и внесён
  в код. Приняты **AR-118…AR-127** — целевая функция на целых штрафах, восемь
  маркеров качества с явными верхними границами, автопроверка инвариантами
  `I-1…I-8`, локальный поиск с доказанным завершением, ручной ход с правом
  ухудшить мягкий маркер под подпись, графическая маркировка слоями, снимок как
  единственный источник печати, файла и ссылки, QR как транспорт подписанного
  адреса. Заведены ворота **G-56…G-60**. Спека — `specs/schedule-block/`,
  исполняемая модель — `model/quality.mjs` (свойства Q1…Q12).
- **2026-08-27** — вопрос владельца «качество продукта 50 % от эталонного?»
  вскрыл, что `Π` читается как доля, а локальный минимум — как эталон. Принято
  **AR-128**: нижняя граница штрафа считается арифметически и показывается
  потолком рядом с агрегатом; размен между маркерами раскрывается списком.
  На первой школе граница `Π_LB = 632` против локального минимума 1128 и
  генератора 2080 — правка закрывает 65,7 % устранимого штрафа (перемерено
  2026-08-27 после снятия правила «окно у педагога», AR-135).
- **2026-08-27** — **решение владельца о ручной настройке**: параметры задаёт
  модератор, генерация вправе думать до пяти минут. Приняты **AR-129** (бюджет
  30 с · 2 мин · 5 мин, стратегия — мультистарт), **AR-130** (генерация
  становится асинхронной задачей с поллингом: пять минут не переживает
  HTTP-таймаут), **AR-131** (реестр параметров данными, правило «норму можно
  ужесточить, ослабить нельзя»), **AR-132** (семь новых жёстких ограничений
  H8…H14 и восемь лемм-отказов к ним). Ворота **G-61**. Реестр —
  `specs/schedule-block/35-parameters.md`, 35 параметров на семи шагах.
- **2026-08-27** — **правки владельца по реестру параметров (редакция 2)**.
  **AR-133**: скелет дня — отдельный блок, скелетов бывает больше одного, норма
  первого класса живёт там; календарь задаёт рамки, а не параметры расписания.
  **AR-134**: приоритет и спаренность — ранги 1…6 с регрессией допусков вместо
  флагов. **AR-135**: у педагога окна обязательны — обед жёстко, отдых мягко,
  методические объединения как общее окно группы; маркер `teacherGap` заменён
  двусторонним `teacherRest`. **AR-136**: отказа «нет решения» не существует —
  три ступени диагностики, релаксация отвечает действием. **AR-137**: недельные
  часы выводятся из годовых. **AR-138**: глубина поиска вместо секунд, прогресс
  без цифр. AR-132 вытеснено AR-134.
- **2026-08-27** — **редакция 3 реестра параметров**: разбор владельца вскрыл три
  места, где автор спеки выдал собственные допущения за сказанное владельцем
  либо за норму. Запрет сдвоенных уроков «в 1–4 классах» не был проверен по
  источнику (проверка показала: только 1-е классы, с исключениями) — базис #14
  исправлен, случай записан в разделе «Исправленные строки». Уточнены AR-133
  (скелет привязан к дням недели, обособление и слияние дня), AR-134 (приоритет
  повторяем, задаёт порядок НАЧАЛА урока, продолжение пары вправе выйти за зону;
  спаренность следует за приоритетом; пара делится большой переменой), AR-135
  (норма окон — вывод из нормы завуча и объёма часов). Приняты **AR-139**
  (недельная кривая работоспособности как маркер) и **AR-140** (стабильность
  расписания внутри периода, точечные правки).
- **2026-08-27** — **редакция 4 реестра параметров**. Владелец указал на перекос:
  на согласование уходила математика, которую должен решать исполнитель. Принято
  **AR-141** (владельцу — продуктовые вопросы, формулы обосновывает исполнитель).
  Выведены и закрыты две величины: шкала приоритета 32-16-8-4-2-1 из свойства
  невыгодности размена и норма отдыха как пропорция объёму часов с округлением к
  ближайшему. Спаренность **отвязана** от приоритета (решение владельца: шкалы не
  совпадают, ставится вручную). Режим периода определён как **временный скелет**
  на диапазон дат, а не пересборка сетки. Добавлена выписка требований к кабинету
  завуча — списком, без отдельной спеки.
- **2026-08-27** — **редакция 6 реестра параметров**. Владелец указал на две
  ошибки сразу: норма окон на отдых была невыполнима по построению (завуч ставит
  четыре, у педагога помещается одно — и программа обязана ответить неправду), а
  трудовые нормы вообще не дело завуча. **AR-135 переписан**: обед, окна на отдых
  и предельная занятость убраны из блока и уходят к кадрам и бухгалтерии; маркер
  качества `teacherGap` удалён — окна возникают как следствие расстановки, и
  продукт мнения о них не имеет. Принято **AR-142**: граница блока проходит по
  роли, которая владеет величиной в жизни школы, а не по тому, кому величина
  нужна технически.
- **2026-08-27** — **AR-143: единый словарь**. Владелец указал, что автор спеки
  говорит с ним на языке, которого в продукте нет: «штраф», «вес», «маркер».
  Заведён словарь из десяти понятий — человеку по-русски, внутренние имена
  только в коде. Ворота **G-62** проверяют, что жаргон не попал ни в подписи
  экранов, ни в тексты отказов.
- **2026-08-27** — **редакция 8 реестра параметров**. Владелец разобрал текст
  макета и указал на подмену: сообщение о расхождении с учебным планом стояло
  там, где расхождения быть не может. Приняты **AR-144** (годовая норма — закон
  для сборки; о расхождении система говорит только про правку рукой),
  **AR-145** (час не теряется, а добирается: счёт «положено минус проведено» и
  адресное предложение слота на следующей неделе вместо замены урока) и
  **AR-146** (уход педагога из тенанта включает режим подстраховки на четыре
  недели: предмет снимается, места заполняются, по истечении срока школе
  предъявляется выбор, но решение за неё не принимается). Ворота **G-63**.
- **2026-08-27** — **редакция 9**. Владелец закрыл четыре вопроса реализации,
  и три из них задавать не следовало: ответы уже стояли в его прежних словах
  и в контракте. Приняты **AR-147** (уровень спаренности выводится из годовых
  часов: чем больше уроков, тем выше порог; шкала выведена из свойства, а не
  выбрана) и **AR-148** (расписание настраивает модератор; администратор школы —
  отдельная роль с полным доступом к тенанту, что **отменяет AR-88**). Заодно
  исправлено: `pairingIsAdjacent` несла вторую ветвь, бывшую подмножеством
  первой, — условие выглядело содержательным, ничего не проверяя.
- **2026-08-27** — **редакция 10, `docs/UTC-BOUNDARY.md`**. Владелец указал на
  подмену масштаба: УТЦ — это три компонента (календарь, расписание, журнал),
  а не только генератор, и их механика обязана быть контрактом, отделимым от
  конкретного кабинета — «двигатель, который вставляется в машину». Принят
  **AR-149**, обобщающий AR-142 на всю структуру УТЦ. Найдены и
  запротоколированы два нарушения в коде: учебный календарь без своего права
  (сидит на `schedule.build`), три сервиса компонента вызывают друг друга
  напрямую классами. Причина требования — масштаб экосистемы: Schoolium как
  база для колледжей и вузов не выдержит жёсткой связки с текущими
  кабинетами школы.
