# EduStore — реестр G-проверок

> Дата: 2026-07-28. G-проверка = **исполняемое доказательство инварианта**
> (не «проверено глазами»): скрипт поднимает реальный Nest-контекст + БД и
> перечислением доказывает, что инвариант держится. Утверждение «работает»
> принимается только со ссылкой на зелёную G-проверку.
>
> Формат принят по паттерну shintop (репозиторий недоступен из этой сессии —
> структура выведена из скиллов architecture/review; при расхождении с каноном
> shintop поправить шапку, содержание переносимо). Ссылки АР-N —
> блочные реестры [docs/ar/](./ar/INDEX.md).

## Существующие (зелёные на `a4014cb`)

| G | Команда | Инвариант | Покрывает АР |
|---|---|---|---|
| G-1 | `npm --workspace apps/api run tenant:check` (6/6) | Чтение/запись/`count`/`findUnique` не пересекают границу школы; запрос с тенантом-«не тем» → пусто/`P2025`; аутентифицирован-без-тенанта в guarded-операции → отказ | AR-2 |
| G-2 | `npm --workspace apps/api run minor:check` (10/10) | DM родитель↔свой ребёнок разрешён по ребру; чужой взрослый↔минор запрещён; двойная роль послаблений не даёт; минор↔минор запрещён; external в канал с минором отклонён на добавлении (двусторонне) | AR-27, AR-3 |
| G-3 | `npm --workspace apps/api run comm:check` (20/20) | `mode` не угадывается (`400 MODE_REQUIRED`); suggest-mode advisory (не создаёт сообщение); инвариант миноров срабатывает и на REST-пути; keyset-пагинация; правка ⇒ `edited=true` без стирания; объявление → required-set → ack → overdue → reconcile ушедшего | AR-28, AR-18 |
| G-4 | `npm --workspace apps/api run cascade:demo` | Outbox → 3 подписчика (каналы/питание/УМК); повторная доставка идемпотентна (inbox); петля обрывается depth-guard'ом на MAX_CASCADE_DEPTH | AR-5, AR-21, AR-22 |
| G-5 | `npm --workspace apps/api run parser:check` (13/13) | `doc.file.enriched → textbook.parsed`; payload несёт `fileId`, не `s3Key`; идемпотентность; не-учебник → молчит; пустой `textExtract` → деградация без падения; tenant-изоляция новых таблиц | AR-20, AR-12 |
| G-7 | `npm --workspace apps/api run storage:check` | Живой S3: presign PUT → HEAD → download → сверка контента → delete; пустой конфиг → внятный отказ, не падение | AR-12 |
| G-8 | `npm --workspace apps/api run flow:check` (25) | Сквозной поток «учебник → КТП → КПП → содержание уроков»: авто-контекст загрузки из назначения, живой docs/-контур (`STORAGE_MODE=local`), генерация/дополнение/версионирование КТП, оценка часов, раскладка карт по урокам, fallback llm→regexp, шифрование ключа | AR-38, AR-39, AR-20 |
| G-9 | `node e2e/smoke-textbook.mjs` | Живой Chromium-смок: пилотный QR-вход → загрузка настоящего PDF → скриншоты каждого шага | AR-15, AR-38 |
| G-10 | `npm --workspace apps/api run authz:check` | **RBAC-полнота перечислением**: обход всех контроллеров через ModulesContainer; каждая мутация — `@RequirePermission` либо whitelist-строка с причиной (resource/key/identity-gated); коды сверяются с каталогом и пакетами; протухший whitelist = провал (64 gated / 16 whitelist) | AR-35, AR-7 |
| G-11 | внутри `tenant:check` (кейс 6) | **Tenant fail-closed**: аутентифицированный без школы → `{tenantId:null, system:false}` (не system-обход); guarded-запрос без тенанта → отказ | AR-34 |
| G-12 | структурно: миграция `ar4_unified_journal` + `tsc` | **Единый журнал**: модель `Grade` удалена из схемы — любое обращение не компилируется; метрики/отчёты/голос на `JournalCell`; писатель один (`grade.posted`/`grade.removed`) | AR-4 |
| G-13 | `npm --workspace apps/api run consent:check` (7) | **Consent-гейт**: без `predictive_profiling` — исключён из atRisk с явной пометкой, срез ИОМ → `NO_PROFILING_CONSENT`; согласие открывает, отзыв закрывает; сигналы копятся независимо | AR-29 |
| G-15 | `npm --workspace apps/api run timetable:check` (6) | **Timetable-авторинг сквозной**: сетка через API (не seed) → `INSUFFICIENT_SLOTS` → расширение → Solver `scheduled`; `TIMETABLE_IN_USE` защищает сетку под КПП; `BAD_SLOT` | AR-38 |
| G-16 | `npm --workspace apps/api run events:check` | **Канон событий**: запрет `edustore.*`, все литералы реестров — `<домен>.<агрегат>.<глаголПрош>.v<N>`, wildcard-паттерны валидны; + рантайм-валидация в `newEvent()` (40 литералов) | AR-23, AR-24 |
| G-19 | `node tools/method/lint-ar.mjs` | **Протокол реестров решений**: сквозная нумерация без дыр и дублей, номер внутри диапазона своего блока, диапазоны блоков не пересекаются, `next` в индексе верен, у каждого `[РЕШЕНО]` непустые ворота, все ссылки `AR-*`/`G-*`/`L-*` разрешаются | метод (E-6.1, E-6.3, E-12.3) |
| G-20 | `node tools/method/lint-lenses.mjs` | **Протокол линз**: полнота паспортов, сквозная нумерация, active-линза имеет детектор, каждый пункт чек-листа эволюционной архитектуры связан хотя бы с одной линзой, нет линз-сирот | метод (E-12.1, E-12.2) |
| G-21 | `node tools/method/spec-lint.mjs --self-test` | **Линт спеки работоспособен**: на заведомо плохой фикстуре срабатывают все параметры P1…P7 (линтер, который ничего не находит, — худший вид зелёного) | метод (E-3.4, E-12.4) |
| G-37 | `node tools/design/check-contrast.mjs` + `node tools/design/tokens-to-css.mjs --check` | **Контраст дизайн-токенов перечислением**: 21 пара «текст × фон» из `docs/design/tokens.json` против WCAG 2.1 (текст ≥4.5:1, крупный/UI ≥3.0:1); маркеры пола, все шесть отметок (2–5, «н», «б»), кнопки, состояния. Плюс обход проверки закрыт: литеральных цветов в компонентах контура нет ни одного, а `tokens.css` сгенерирован из тех же токенов и сверен с ними | AR-80, AR-79, AR-42 |
| G-38 | `node tools/method/lint-screens.mjs` | **Экраны против спеки перечислением** (усилена этапом 0: сверка кодов ошибок идёт в обе стороны — код, названный на экране без строки в реестре §9, роняет проверку): у каждого состояния FSM есть экран-дом; каждый экран объявляет маршрут и роли; события экранов существуют в контракте спеки; каждый код ошибки объявлен и использован; обязательные состояния loading/empty/error присутствуют | AR-69, AR-72, AR-77 |
| G-64 | `npm --workspace apps/api run launch:check` | **Вход по юзернейму и паролю** (AR-154/AR-156): креды от модератора открывают сессию без QR; неверный пароль и несуществующий юзернейм — один и тот же `LOGIN_FAILED`, существование учётки не раскрывается | AR-154, AR-156 |
| G-65 | внутри `launch:check` и `access:check` | **Отзыв активации** (AR-153): сессии учётки закрыты, карточка снова «не авторизована», повторная активация проходит; доказано на персонале, учениках | AR-153 |
| G-66 | `npm --workspace apps/api run moderator:check` | **Матрица «роль × мутации» v2 перечислением**: админ проходит все гейченные мутации, модератор — ровно КПЦ, завуч — ровно панель УТЦ, читающие роли — ни одной, родитель/ученик — одна проекция `diary.read` | AR-150, AR-151, AR-152 |
| G-67 | внутри `launch:check` | **Дневник и средние — проекции без записи**: у контроллера дневника ноль мутаций (обход через ModulesContainer); чужой ребёнок — `ACCESS_REVOKED` без раскрытия существования; объём решает идентичность (`GuardianLink`), не роль | AR-158, AR-159, AR-151 |
| G-69 | внутри `launch:check` | **Данные-минимум детей**: перечень колонок записи ученика фиксирован перечислением против information_schema — новое ПДн-поле роняет ворота до решения владельца | AR-155 |
| G-70 | внутри `launch:check` | **Пресет предметов идемпотентен**: повторный прогон — ноль дублей, ручные карточки не затёрты | AR-160 |
| G-39 | `node tools/method/lint-screens.mjs` | **Адаптация описана перечислением**: каждый из 21 экрана присутствует в таблице раскладок `75-adaptive.md` §6; у каждой из 15 модалок задано поведение и на десктопе, и на мобайле; реестр кнопок полон; объявлены обе точки останова и правило горизонтальной прокрутки широких таблиц | AR-81, AR-82 |
| G-40 | `node tools/method/lint-agents.mjs` | **Инструкции агентов против конвейера**: каждая стадия С0…С7 закреплена за агентом; каждая инструкция содержит восемь обязательных разделов (роль, вход, что делает, выход, ворота, запреты, типовые ошибки, передача); каждая перечислена в README и наоборот; все ссылки на пути репозитория разрешаются | метод (E-6.1, E-12.2) |
| G-24 | `npm --workspace apps/api run access:check` (27) | **Контур доступа без SMS перечислением**: регистрация по QR создаёт `User`+`Membership`+сессию 90 дней; телефон уникален на инсталляцию; регистрация во второй школе не конфликтует; bypass fail-closed при неизвестном `AUTH_MODE`; SMS-маркеров, зависимостей и env-переменных в кодовой базе нет | AR-46, AR-47, AR-66, AR-94 |
| G-33 | `npm --workspace apps/api run onboarding:check` (25) | **Онбординг сквозной**: путь `empty → ready` пройден мастером; у каждого шага есть выход вперёд и возврат для правки; возврат данных не стирает; повторный прогон мастера отклонён; правка контингента после `ready` не роняет сетку, открепление педагога роняет | AR-72, AR-77, AR-85 |
| G-34 | `npm --workspace apps/api run generator:check` (13) | **Генератор доказуемо честен**: 160 ячеек сетки первой школы проверены перечислением — окон, полуокон и двойной занятости нет, нагрузка выполнена полностью, групповые часы стоят спаренными единицами; каждый из девяти кодов отказа воспроизводится, восемь — до перебора | AR-73, AR-75, AR-84 |
| G-35 | `npm --workspace apps/api run schooljournal:check` (18) | **Журнал производен**: колонки = материализованные уроки (два урока в дату → две колонки); отметка в будущий урок отклонена контрактом; «н» и «б» не входят в средний балл; деактивация сохраняет историю; строки приезжают событиями контингента, удаление ученика их снимает | AR-74, AR-78, AR-79, AR-83 |
| G-36 | `npm --workspace apps/api run qr:check` (21) | **QR-активация**: токен одноразов и привязан к своей карточке; закрытие карточки гасит QR; повторный скан — `TOKEN_USED`; статус читается поллингом раз в 2 секунды вместе с идентичностью сканировавшего; активация и привязка — в аудите; OTP не участвует нигде | AR-76, AR-87, AR-94 |
| G-41 | `npm --workspace apps/api run moderator:check` (28) | **Полные права модератора и аудит**: перечислением по мутациям контура — модератор проходит все, четыре читающие роли ни одной, педагог только свои уроки; гейт `LESSON_NOT_HELD` срабатывает и для модератора; все 22 события версии ложатся в аудит с идентичностью | AR-88, AR-30 |
| G-42 | `npm --workspace apps/api run regen:check` (16) | **Регенерация не теряет историю**: урок с отметками отвязывается (`detachedAt` + `schedule.lesson.detached.v1`), урок без отметок исчезает молча; журнал узнаёт об отвязке событием и отклоняет и постановку, и снятие кодом `LESSON_DETACHED`; правка контингента сетку не пересобирает; событие, случившееся ДО генерации и доехавшее через outbox после confirm, свежую сетку НЕ роняет — устаревание только от изменений после генерации | AR-85, AR-74 |
| G-43 | `npm --workspace apps/api run reversal:check` (28) | **Обратимость и каскад удаления**: у каждой операции реестра есть обратная либо причина; каскад снимает привязки, роняет сетку в `stale` и сохраняет отметки; `LAST_MODERATOR` и `LAST_ROLE` держат школу и сотрудника; роль модератора выдаётся и снимается той же кнопкой | AR-89, AR-90, AR-78, AR-102 |
| G-44 | `npm --workspace apps/api run login:check` (23) | **Маршрут входа перечислением**: восемь клеток решётки «устройство × якорная сессия × камера × присутствие модератора»; токен привязки одноразов (3 мин) и наследует школу и роли; код входа одноразов (5 мин, 6 цифр); деактивация отзывает все сессии, адресное завершение — одну; bootstrap-ссылка одноразова и перевыпускается; `next` не даёт открытого редиректа | AR-91, AR-92, AR-93, AR-94 |
| G-45 | `npm --workspace apps/api run calendar:check` (16) | **Календарь и материализация**: справочник нерабочих дней загружен, год без данных даёт `CALENDAR_YEAR_MISSING`; уроков в выходные и праздники нет; повторный прогон создаёт ноль записей; три триггера названы; горизонт три недели; `TERM_OVERLAP`/`TERM_REVERSED` воспроизводятся | AR-100, AR-101, AR-73 |
| G-46 | `npm --workspace apps/api run daygrid:check` (19) | **Дневная сетка и бюджет перебора**: «уроков в день» — второй множитель слотов недели; `DAY_EXCEEDS_SANPIN` и `DAY_TOO_LONG` до перебора; каждый из четырёх временных параметров влияет на длину дня; бюджет 20 с / 200 000 попыток исчерпывается честным `NO_SOLUTION`; генерация детерминирована при фиксированном зерне | AR-103, AR-107 |
| G-47 | `npm --workspace apps/api run schemafit:check` (41) | **Стык с физической схемой**: 11 доменных таблиц с ключом тенанта; три занятых имени разведены префиксом `School*`, legacy не тронут и сохранил свой автомат; `User.phone`, отчество, аватар и массив ролей `Membership` заведены; в миграции версии ни одного `ALTER` по таблицам КТП/КПП | AR-104, AR-99 |
| G-48 | `npm --workspace apps/api run destructive:check` (25) | **Обратимость разрушающего**: каждая разрушающая операция стоит в реестре своей строкой, необратимых пять; ответ карточки класса несёт `filledProfiles` отдельно от `totalProfiles`; удаление класса и ученика с отметками отклонено контрактом, а не скрытием кнопки | AR-105, AR-90 |
| G-49 | `npm --workspace apps/api run secondschool:check` (12) | **Вторая школа в той же базе**: три ветки регистрации по телефону неразличимы снаружи по форме и времени; телефон из другой школы даёт второе членство; изоляция держится при двух workspace; константы «школы по умолчанию» и чтения «первой школы из таблицы» в коде версии нет | AR-106, AR-47, AR-98 |
| G-50 | `npm --workspace apps/api run subscribers:check` (78) | **События полны по подписчику**: 22 события, у каждого издатель, подписчик и реакция; «нет подписчика (только аудит)» объявлено явно и подтверждено подпиской аудита; каждая названная подписка существует в шине; удаление ученика снимает строку журнала подпиской | AR-108, AR-24, AR-45 |
| G-51 | `npm --workspace apps/api run concurrent:check` (12) | **Одновременная правка**: четыре мутации несут версию агрегата, вторая запись поверх изменённого состояния отвечает `CONCURRENT_EDIT` с именем второго модератора; остальные мутации адресны, и последняя запись побеждает; повторный скан QR — `TOKEN_USED` | AR-109, AR-102 |
| G-52 | `node tools/method/check-testids.mjs` | **Реестр элементов реализован поимённо**: каждый `data-testid` из посекционных таблиц экранов `70-screens.md` присутствует в модуле своего экрана; ни один модуль не несёт идентификаторов чужого экрана; каждый элемент оболочки (`L.*`, реестр `75-adaptive.md` §2) стоит в `shell.tsx` — **обе раскладки**: восемь десктопных и пять мобильных (`L.header.*`, `L.tabbar`) | AR-115, AR-81, AR-82 |
| G-53 | `npm --workspace apps/web run smoke:onboarding` (49 шагов) | **Полный онбординг проходит в браузере**: Chromium ведёт пустую школу от bootstrap-ссылки до подтверждённой сетки, **выставленной отметки и заполненной темы урока**, снимая скриншот каждого шага; на каждом экране проверяются его идентификаторы из таблиц реестра; **каждая из 15 модалок реестра §3 открыта и закрыта**. Учебный день зафиксирован `SCHOOL_TODAY` (AR-117) — иначе объём проверенного менялся бы с календарём: до 1 сентября все уроки будущие и ни одна ячейка журнала не кликается | AR-81, AR-95, AR-18, AR-117 |
| G-55 | `npm --workspace apps/web run smoke:onboarding:mobile` (49 шагов) | **Тот же сценарий в раскладке 390×844**: тот же смок, тот же путь, ветвится только объявленное реестром `75-adaptive.md` (оболочка §2.2, `S-01`, `S-70`). Сверх десктопного доказывает три вещи: **`body` не скроллится по горизонтали ни на одном из 49 экранов** (виновник называется поимённо), **тап-мишени ≥44px перечислением** на каждом ключевом экране, **все 15 модалок открыты и закрыты в мобильной форме** (`fullscreen`/`sheet`), таб-бар уходит под полноэкранный поток и возвращается | AR-81, AR-82, AR-69 |
| G-54 | `npm --workspace apps/api run texts:check` (128) | **Тексты отказов дословны §9 `70-screens.md`**: множества кодов сходятся в обе стороны; шаблон каждого кода, подставленный цифрами из примера §9, даёт ровно текст §9; заглушек («произошла ошибка») нет, и детали действительно попадают в текст, а не игнорируются шаблоном | AR-40, AR-114 |

| G-56 | `npm --workspace apps/api run quality:check` (88) | **Квалиметрия расписания состоятельна**: семь правил удобства дают целые неотрицательные штрафы при положительных верхних границах, `Qᵢ ∈ [0,1]`, свёртка `Π` — неотрицательное целое; веса целые (на этом стоит доказательство завершения поиска); верхняя граница разброса `maxSpread` совпадает с максимумом по **всем** разбиениям малой задачи, а не берётся оценкой сверху; маркер стабильности без подтверждённой сетки неактивен, а не равен единице; **нижняя граница `Π_LB` не выше достигнутой `Π`** и показывается потолком рядом с агрегатом — число без шкалы прочитать нельзя | AR-118, AR-119, AR-123, AR-128 |
| G-57 | там же | **Автопроверка ловит подделку**: инварианты `I-1…I-8` держатся на всех 112 часах сетки первой школы, и каждый из пяти способов испортить допустимую сетку воспроизводит именно свой инвариант — набор проверяется на отказе, а не только на честном входе | AR-120 |
| G-58 | там же | **Автокорректировка завершается доказуемо**: `Π` не растёт (2080 → 1128 на первой школе), каждый принятый ход уменьшает её строго — последовательность в `ℤ≥0` обрывается независимо от таймера; поиск встаёт в локальном минимуме, повторный прогон не делает ни одного хода; два прогона на одном входе дают одну сетку; час с признаком «правка человека» в окрестность не входит; **размен раскрыт** — маркеры, ухудшившиеся при падении общей `Π`, возвращаются списком, а не прячутся за агрегатом | AR-121, AR-128 |
| G-59 | там же | **Ручной ход не переступает жёсткое и называет мягкое**: ход в занятую позицию отклонён инвариантом, ухудшающий допустимый ход найден перечислением окрестности и назвал маркер с величиной, применение рукой ставит признак `manual`; перенос обратим, обмен самообратен | AR-122, AR-90 |
| G-60 | там же | **Снимок один, ссылка подписана**: печатная, табличная и календарная проекции сходятся со снимком по числу ячеек; календарь выражает неделю правилом `RRULE` с `EXDATE`, а не списком уроков; ссылка области «педагог» не выдаёт сетку других педагогов; подпись воспроизводима и меняется вместе с версией сетки, областью и секретом; истёкшая и отозванная ссылки отвечают одинаково | AR-124, AR-125, AR-126, AR-127 |

| G-61 | там же | **Реестр параметров полон и не ослабляет норму**: 16 параметров на пяти шагах, идентификаторы уникальны, у каждого область, дефолт внутри неё и **названный потребитель** (ограничение, маркер либо проекция) — мёртвого ввода нет; каждый вход генератора покрыт параметром; каждый параметр с нормируемым потолком не превышает норму и называет её источник; каждый названный отказ существует в перечне кодов; величины календаря и скелета дня расписанием не собираются; ранги приоритета и спаренности заданы монотонной регрессией с жёсткими краями; **кода `NO_SOLUTION` в перечне нет**, диагностика отвечает действием; глубина поиска измеряется вариантами, а не секундами; прогресс не показывает цифр | AR-129…AR-142 |

| G-62 | там же | **Человек не видит жаргона**: словарь блока содержит десять понятий, у каждого слово для человека, имя в коде и объяснение; названия правил удобства, названия запретов, подписи параметров на экранах и тексты всех отказов перечислением проверены на список запрещённых слов («штраф», «вес», «маркер», «инверсия», «свёртка», «агрегат», «локальный», «релаксация», «эвристика», «окрестность») | AR-143 |

| G-63 | там же | **Годовая норма держится сборкой, а час не теряется**: признак «план — закон для сборки» поднят, и текст расхождения с планом существует **только** для правки рукой — после автоматической сборки такого сообщения нет; недобор, ровный счёт и забег вперёд считаются одной величиной «положено минус проведено»; вместо «урок пропал» предложение называет предмет, класс, дату и номер урока следующей недели, а при заполненной неделе говорит именно это; режим подстраховки длится четыре недели, по истечении называет накопленный недобор в часах и оставляет решение школе — система ни учебный план не правит, ни предмет из него не удаляет | AR-144, AR-145, AR-146 |

**Ворота блока «Расписание» (G-56…G-63)** чисты от БД и сети: они перечисляют
поведение слоя на его же функциях. Прогон 97 утверждений — в
`specs/schedule-block/40-bench.md`, раздел «Прогон ворот».

**В CI**: G-1…G-5, G-8…G-13, G-15, G-16 и ворота версии 1.1.1 (G-24, G-33…G-36,
G-41…G-51, G-54), G-53 и G-55 — job `flow-smoke`; G-19…G-21, G-37…G-40 и G-52 — job `method-lint`; G-56…G-63 — job `quality-gates` (бегут без
БД и без установки зависимостей). Вне CI остаётся только G-7 (нужен живой S3).
Все ворота этапа 1 одной командой: `npm --workspace apps/api run schoolium:check`.

## Недостающие (завести вместе с закрытием соответствующих узлов)

| G | Инвариант, который надо доказать | Условие появления | АР |
|---|---|---|---|
| G-68 | **Миграционный контракт Флёруса**: таблица `specs/school-launch/10-identity.md` §8 сверяется с фактическими схемами обеих систем; тестовый экспорт N учёток проходит валидацию правил Флёруса без конверсии | перед исполнением миграции; сверка таблицы — при каждом изменении модели учётки | AR-157 |
| G-14 | **Контрактная сверка фронт↔бэк — остаток**: structure/timetable уже в `packages/shared` (импортируются обеими сторонами, дрейф ломает tsc; хардкод `x-florus-user-id` удалён); остаток — edu/doc/pilot DTO + типизация engine-контроллера тем же паттерном | следующий инкремент AR-36 | AR-36 |
| G-17 | **DLQ-replay**: событие, добитое до `FAILED`, переигрывается командой; повторная обработка идемпотентна (inbox) | вместе с вводом NATS или ранее | AR-22, AR-24 |
| G-22 | **Иерархия тенантов**: `worknet` не является вторым ключом изоляции — школа-сестра той же сети не видна из workspace; агрегация по сети собирается явным перечислением школ, а не ослаблением tenant-guard; владелец сети не обходит изоляцию | вместе с экраном владельца сети | AR-44, AR-53 |
| G-23 | **Контингент — единственный владелец**: перечислением по всем модулям доказано, что ни один не обращается к таблицам контингента напрямую; все чтения идут через сервис или события; контракт событий контингента версионирован | вместе с выделением контингента за контракт | AR-45, AR-52 |
| G-25 | **Авторство `parenthood`**: писатель один (модуль контингента); неподтверждённое ребро не открывает приватный DM; инвариант миноров (G-2) держится на новом источнике ребра | вместе с реализацией AR-48 | AR-48 |
| G-26 | **Замена способа входа не трогает домен**: доменные модули не импортируют детали авторизации; подмена адаптера за портом `IdentityProvider` не меняет резолв RBAC | вместе с реализацией AR-50 | AR-50 |
| G-27 | **Разделение обязанностей по человеку, а не по роли**: пользователь, несущий одновременно «преподаватель» и «заместитель по учебной работе», не утверждает собственный КТП; перечислением по всем парам «автор ↔ утверждающий» | вместе с ролевой моделью v1 (AR-60) | AR-61, AR-19 |
| G-29 | **QR-подтверждение и fallback**: одноразовость, срок жизни, привязка к конкретной операции; отсутствие второго устройства не делает операцию невыполнимой (код на телефон даёт тот же результат) | вместе с реализацией AR-63 | AR-63 |
| G-30 | **Смена телефона не рвёт связи**: история, членства и согласия остаются на `User.id`; старый номер освобождается только после подтверждения; прерванная на середине смена не оставляет пользователя без доступа | вместе с реализацией AR-64 | AR-64 |
| G-31 | **Календарь — единственный владелец периодов**: ни расписание, ни журнал не хранят собственных дат периодов; перенос рабочей субботы виден обоим модулям без ручной синхронизации | вместе с календарём учебного года | AR-68 |
| G-32 | **v1 без детских аккаунтов**: перечислением — ни один сценарий не создаёт вход ученику или родителю; consent-гейт выключен, но не удалён, и его включение не меняет контракты выдач | предмет вытеснен AR-155 (вход детей и родителей в 1.2.0 при данных-минимум); содержательная часть переезжает в G-69 | AR-70, AR-29 |

## Снятые

| G | Почему снята |
|---|---|
| G-18 | **OIDC-цикл вживую** — предмет исчез: решения AR-1 и AR-33 вытеснены (AR-46, AR-49, AR-57), внешний IdP в Schoolium не используется. Снята решением, а не «подгонкой»: правило 2 ниже соблюдено. |
| G-28 | **Модератор не завершает операции над чужим аккаунтом в одиночку** — предмет вытеснен: решением владельца 2026-08-21 модератор получил полные права школы (AR-62 → AR-88). Противовес перенесён в аудит: доказывается G-41 |
| G-6 | **Пилотный контур (invite→QR→вход→назначение)** — предмет исчез: пилотных пользователей нет, регистрация проходит сразу (AR-65). Проверка была зелёной; её содержательная часть (одноразовость токена, привязка операции к QR) переезжает в G-29, а не теряется. |

Строки реестра, ссылающиеся на снятые ворота (AR-1, AR-14, AR-15, AR-33), сохраняют
ссылку: реестр — журнал, а не текущее состояние.

## Правила ведения

1. Новый инвариант в АР-реестре со статусом [РЕШЕНО] обязан либо ссылаться на существующую G-проверку, либо заводить строку в «Недостающих» в том же коммите. Проверяется машинно — G-19.
2. G-проверка падает → мерж запрещён; чинится код или явным решением меняется АР (с правкой реестра), но не «подгоняется» проверка.
3. G-7 (живой S3) остаётся ручным прогоном перед релизом; всё остальное — в CI.
