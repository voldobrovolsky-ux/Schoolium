# С5 · Отчёт стенда

Уровень: **T2** (заявлен в `00-request.md` до прогона). Модель: `model/`
(`walk.mjs`, `props.mjs`, `sim.mjs`) — все зелёные на момент сдачи; история
находок ниже.

## Прогоны

| Прогон | Что перечисляли | Результат |
|---|---|---|
| Обход FSM (`walk.mjs`) | 13 состояний × 25 переходов: достижимость, тупики, путь к терминалу, дом, актор | 0 дыр; терминал `ready` достигается только действием человека (AR-18) |
| Свойства (`props.mjs`) | мастер, алфавит (Ё=Е, отчество опц.), группы, валидация нагрузки, генератор на данных первой школы (280 ячеек), материализация с праздниками, гейт журнала, деактивация | 16 проверок зелёные после 4 правок спеки (см. находки) |
| Симуляция (`sim.mjs`) | касания модератора на полном онбординге 8×15; зонд цены нового элемента | ~971 касание ≈ 65 мин ≤ порога 90 мин; контингент доминирует (40 мин) |

## Находки → правки (история, не итог)

| # | Находка (свидетель — прогон модели) | Куда ушла правка |
|---|---|---|
| 1 | **Часы групп одного предмета никем не выравниваются**: два педагога вписывают в свои окошки разные часы (3 и 1) — полуокно неустранимо на уровне арифметики, генератор обречён | Спека, экран 2: валидация «часы групп предмета равны», код отказа `GROUP_HOURS_UNEQUAL` |
| 2 | **Планирование групп независимыми единицами не сходится**: первая версия генератора не нашла сетку ни на одном из 40 зёрен; после перехода на атомарную спаренную единицу (обе группы в одном слоте, два педагога) — сетка со 2-го зерна | Спека, ограничение 4 генератора: спаренная единица — требование к реализации, не деталь |
| 3 | **NO_SOLUTION вместо диагноза**: перегруз педагога обнаруживался перебором, а не арифметикой — модератор ждал бы полный перебор ради бесполезного ответа | Спека: арифметические отказы (6 кодов) вычисляются до запуска перебора |
| 4 | **«Текущий урок» неоднозначен**: урок сегодня в 14:00 — можно ли ставить отметку в 9:00? | Спека, журнал: гейт по дате дня [дефолт], время слота — кандидат 1.1.x |
| 5 | **Регенерация — единственный способ внести предмет среди года** (зонд L-10): добавление предмета трогает 4 точки и пересобирает всю сетку | Слот в спеке уже был (ручная правка сетки); зонд подтвердил его приоритет для 1.1.x |

## Дополнение этапа 0 (2026-08-21)

Аудит перед кодом добавил в модель три свойства; каждое сперва прогонялось
красным на прежнем поведении спеки, затем зелёным на исправленном
(отчёт — [81-audit-stage0.md](./81-audit-stage0.md)).

| # | Находка (свидетель — прогон модели) | Куда ушла правка |
|---|---|---|
| 6 | **Регенерация уничтожала историю**: журнал подписан только на материализацию, об исчезновении урока не узнавал; урок с отметками пересобирался вместе со всеми | `30-spec.md` «Жизненный цикл сетки», событие `schedule.lesson.detached.v1`, AR-85, ворота G-42, свойство P8 |
| 7 | **Любая правка после `ready` роняла сетку в `stale`**: приём одного ученика предлагал регенерацию всей школы | таблица «Что делает сетку устаревшей» в `30-spec.md`, `editEffects` в `model/states.mjs`, свойство P9 |
| 8 | **Класс из одного ученика делился на группы**: мастер принимал 2 группы при 1 ученике, генератор планировал урок пустой группе | правило шага 4 мастера, `wizard.groupsFit`, свойство P10 |

| 9 | **Полные права модератора против гейта даты** (решение владельца по В3): нужно было различить «нет права» и «урок ещё не прошёл» — иначе полномочия обошли бы факт календаря | `30-spec.md` «Журнал» и права, AR-88, ворота G-41, свойство P11 |

| 10 | **Операции без обратной**: перечисление парами «действие → выход назад» показало пять дыр — удаление класса и предмета объявлены в FSM без контракта, реактивации ученика и сотрудника нет вовсе, снятие роли отсутствует | реестр обратимости в `30-spec.md`, AR-90, ворота G-43, свойство P12 |
| 11 | **Удаление сотрудника без каскада и без защиты школы**: удалить можно было единственного модератора, а привязки и сетка оставались висеть | раздел «Удаление, деактивация, реактивация», AR-89, `staffRemoval`, свойство P13 |
| 12 | **Один код на две причины**: отвязанная колонка отвечала `LESSON_NOT_HELD` — «урок ещё не прошёл» вместо «урока больше нет в расписании» | код `LESSON_DETACHED`, §9 экранного реестра |

| 13 | **Регистрация не заканчивалась сессией**: `staff/join/:token` заводил запись, но не пускал человека в кабинет — стенд не ловил, потому что перечислял операции, а не устройства | AR-91, раздел «Маршруты входа», свойство P14 |
| 14 | **Обещанный резервный вход не существовал**: спека отправляла к модератору «за QR активации», а тот одноразов и сгорел при регистрации | AR-92 — код входа (QR и шесть цифр), экран `S-05`, ворота G-44 |
| 15 | **У графа онбординга не было корня**: первый модератор школы не появлялся ниоткуда | AR-93 — платформенный bootstrap с одноразовой ссылкой |
| 16 | **Деактивация не отзывала сессию**: доступ уволенного жил бы ещё 30 дней | AR-92, событие `staff.session.revoked.v1`, свойство P14 |

| 17 | **Решётка входа перечислена заново без SMS** (решение владельца: провайдера не покупаем): пять клеток изменились — привязка устройства по QR стала штатным маршрутом нового устройства, код от модератора — восстановлением; клетка «единственный модератор без телефона» была вечным тупиком, закрыта перевыпуском bootstrap-ссылки | AR-94, `loginRoute` переписан, свойство P14 (красный прогон: 5 падений) |
| 18 | **FSM привязки устройства**: одноразовость токена, TTL 3 минуты, наследование школы и ролей сканирующего, отказ деактивированному сканеру, адресный отзыв сессии | AR-94, `deviceLink`, свойство P15 |

| 19 | **Источник праздников не был назван**: материализация «пропускает праздники», а откуда их берёт — нигде; кодер захардкодил бы 2026 год | AR-100, справочник по годам, отказ `CALENDAR_YEAR_MISSING`, свойство P16 |
| 20 | **Триггер скользящей материализации отсутствовал**: горизонт «3 недели» двигать было некому | AR-101 — идемпотентность и три названных триггера, свойство P16 |
| 21 | **Второго модератора завести нечем**: `S-30` не имеет секции модераторов, а `LAST_MODERATOR` подразумевает, что их бывает больше одного | AR-102 — выдача роли на `S-31`, отказы `LAST_MODERATOR` и `LAST_ROLE`, свойство P17 |
| 22 | **Таблицы контура доступа не перечислены**: инвентарь знал 11 доменных моделей, а сессии, токены и коды не существовали ни в одном списке | AR-99 — восемь таблиц с правилом изоляции, состояние привязки в БД |

## Что стенд НЕ ловит (по построению)

1. ~~**Время внутри дня**~~ — **закрыто аудитом перед кодом (AR-103).** Слепая
   зона оказалась глубже, чем «модель считает слотами»: числа уроков в день не
   собирал ни один экран, хотя оно второй множитель «слотов недели» в двух
   арифметических отказах. Заведены поле `S-41.input.slotsPerDay`, дневной
   потолок параллели (базис #11, `DAY_EXCEEDS_SANPIN`) и проверка длины дня
   (`DAY_TOO_LONG`, 420 мин — продуктовый дефолт, не норма). Свойство P18,
   ворота G-46.
2. **Конкурентность** — частично закрыта решением (AR-109), стенд по-прежнему
   однопоточен. Исход названен: версия агрегата на четырёх мутациях →
   `CONCURRENT_EDIT`; скан в момент закрытия карточки — `TOKEN_USED` по
   одноразовости токена. Строка написана **до** AR-102, сделавшего двух
   одновременных модераторов нормой, — к ней после этого никто не возвращался.
   Прогон живой гонки остаётся на T3, ворота G-51.
3. **Реальная комбинаторная сложность**: моя модель — жадный перебор с
   рестартами на 8 классах; на 32 классах с литерами сходимость не доказана.
   Требование о таймауте **больше не висит в воздухе**: бюджет назван числом
   (20 с либо 200 000 попыток) и закреплён воротами — AR-107, G-46. Сама
   сходимость на 32 классах остаётся недоказанной: это прогон реализации.
4. **Пользовательское поведение**: 65 минут онбординга — расчёт по касаниям, не
   наблюдение; реальный модератор отвлекается, ошибается, возвращается.
   Проверяется только пилотом первой школы.
5. **Содержимое QR и безопасность канала** (перехват кода с экрана): модель
   проверяет одноразовость логически, не криптографию. Вход — security-review
   реализации.
