# Промпты агентам по волнам — блок «Расписание» (УТЦ)

> Подчинённый документ [`80-code-prompt.md`](./80-code-prompt.md). Каждый промпт
> самодостаточен: агент, получивший только его и ссылку на ветку, выполняет свой
> этап без дополнительных объяснений. Ловушки вписаны в тот промпт, где они
> сработают, — не в общий список, который агент пролистает.
>
> Волны и зависимости — §5 [`50-implementation.md`](./50-implementation.md).

---

## Общая шапка (подставляется в каждый промпт)

```
Репозиторий voldobrovolsky-ux/EduStore, ветка claude/schedule-engine-implementation-utc.
Монорепо: NestJS+Prisma API (apps/api), React/Vite web (apps/web), контракты TS (packages/shared).

Читай перед работой: specs/schedule-block/50-implementation.md (§3 ловушки — целиком),
30-spec.md, 35-parameters.md. Решения: docs/ar/domain.md, AR-118…AR-148. Ворота: docs/G-CHECKS.md.

ПЯТЬ ПРАВИЛ:
1. Расхождение со спекой — сначала правка спеки с обоснованием, потом код. Молча не обходить.
2. Человек не видит жаргона. Запрещённые ПОДСТРОКИ в человеко-видимых строках:
   'штраф', 'вес ' (с пробелом), 'маркер', 'инверси', 'свёртк', 'агрегат', 'локальн',
   'релаксац', 'эвристик', 'окрестност'. Внутри кода допустимы. Ворота G-62.
3. Прогресс сборки — ни одной цифры (PROGRESS_SHOWS_NUMBERS = false, AR-138).
4. Отказа «не удалось, попробуйте ещё раз» не существует: адрес, имя, действие.
5. Красные ворота — этап не сдан. Проверку не подгонять под код.

НЕ ДОЛЖНЫ ПОКРАСНЕТЬ: G-33, G-34, G-42, G-45, G-46, G-53, G-55.

Отчёт в конце: этап, изменённые пути, команды ворот со статусом числами,
что осталось открытым и почему.
```

---

## Волна 1 · два агента параллельно

### А-Э1 · Схема: происхождение слота, версия сетки, устойчивое чтение

```
ЗАДАЧА: Э1 из specs/schedule-block/50-implementation.md.

Строишь:
1. TemplateSlot (apps/api/prisma/schema.prisma:1362): origin String @default("generated"),
   movedBy String?, movedAt DateTime?.
2. ScheduleTemplate (:1340): version Int @default(1), penalty Int?, penaltyFloor Int?;
   комментарий статуса переводится на пять значений (draft | repaired | edited |
   confirmed | stale) — сами статусы вводит Э7, ты готовишь место.
3. Миграция по образцу apps/api/prisma/migrations/20260821090000_schoolium_111_onboarding/.
4. Чтение шаблона (apps/api/src/schoolium/schedule/schedule.service.ts:353) получает
   orderBy: [{dayNo},{slotNo},{classId},{groupNo}].
5. Запись слотов (:325) проставляет origin.
6. Признак доходит до конца цепочки: TemplateSlotDto (packages/shared/src/schoolium.ts:462),
   preview(), и SchoolLesson (schema.prisma:1383).

ЛОВУШКА, РАДИ КОТОРОЙ ЭТОТ ЭТАП ИДЁТ ПЕРВЫМ:
unitsFromSlots (quality.ts:94-100) присваивает часу порядковый номер ПО ПОРЯДКУ ПРИХОДА
СТРОК. Чтение шаблона идёт без orderBy. Значит идентификатор часа плывёт между двумя
чтениями. От него зависят три вещи из разных этапов: правило стабильности
(quality.ts:303-309), журнал ходов и unitId, приходящий с клиента в ScheduleMove.
Без orderBy этапы Э5, Э7 и Э12 будут измерять шум.

ВТОРАЯ ЛОВУШКА: признак origin обязан пройти ВСЮ цепочку. Если он есть в БД, но нет
в TemplateSlotDto — метка «рука» видна в предпросмотре и исчезает после подтверждения.
Если его нет в SchoolLesson — исчезает после материализации.

ТРЕТЬЯ: любая НОВАЯ модель обязана попасть в apps/api/src/common/tenant/tenant-models.ts.
Не перечисленная модель не фильтруется по школе молча. В этом этапе новых моделей нет,
но правило действует для всех последующих.

ДОКАЗАТЕЛЬСТВО:
npx prisma validate && npm --workspace apps/api run prisma:generate
npm --workspace apps/api run schemafit:check
npm --workspace apps/api run regen:check
npm --workspace apps/api run onboarding:check
```

### А-Э2 · Три кода отказа блока в контракте версии

```
ЗАДАЧА: Э2 из specs/schedule-block/50-implementation.md.

Вносишь В КОНТРАКТ ВЕРСИИ ровно три кода: INVARIANT_BROKEN, MOVE_REJECTED, MOVE_DEGRADES.
Коды выдачи (SNAPSHOT_NOT_FOUND, SHARE_*, EXPORT_FORMAT_UNSUPPORTED) — НЕ ТРОГАЕШЬ,
они ждут Э12. Коды §7.5 (MAKE_UP_*, COVER_MODE_*, PLAN_DIVERGES_BY_HAND) — ждут Э13.

ПОРЯДОК ОБЯЗАТЕЛЕН ЦЕЛИКОМ, иначе падают G-54 и G-38 (оба сверяют множества В ОБЕ СТОРОНЫ):
1. код в packages/shared/src/schoolium.ts (ERROR_CODES, :110-142);
2. текст в apps/api/src/schoolium/schoolium.errors.ts (TEXTS, :18) — ДОСЛОВНО из
   packages/shared/src/schedule-quality.ts:297-300; статус в :59 — оба MOVE_* это 409;
3. строка в specs/school-onboarding/70-screens.md §9 (начало :653), дословно;
4. образец деталей в apps/api/scripts/error-texts-check.ts (SAMPLE, :43) — тип
   Record<ErrorCode, …>, БЕЗ ОБРАЗЦА НЕ КОМПИЛИРУЕТСЯ.
Плюс: lint-screens.mjs:63-68 требует, чтобы код встречался в описании экрана НЕ МЕНЕЕ
ДВУХ РАЗ.

ЛОВУШКА, КОТОРАЯ УБЬЁТ ВЕРСИЮ, ЕСЛИ ЕЁ НЕ ЗНАТЬ:
Соблазн «слить SCHEDULE_BLOCK_ERRORS в ERROR_CODES одним движением» — запрещён.
SCHEDULE_BLOCK_ERRORS содержит ВОСЕМЬ СНЯТЫХ кодов, заведённых AR-132 (он помечен
[заменено AR-134]) и убранных редакцией 6: SUBJECT_MAX_PER_DAY_UNREACHABLE,
TEACHER_MAX_PER_DAY_UNREACHABLE, TEACHER_NO_GAPS_UNREACHABLE, GROUP_EDGE_UNREACHABLE,
SUBJECT_POSITION_IMPOSSIBLE, TEACHER_UNAVAILABLE_OVERBOOKED, PAIRED_HOURS_ODD,
PAIRED_FORBIDDEN_FIRST_GRADE. Слияние вернёт в версию отказы про обед и «без окон»,
которых блок не задаёт (AR-135), и G-54 потребует для каждого дословный текст в §9.

ВТОРАЯ ЛОВУШКА: в контракте живут ПАРЫ ИМЁН ОДНОГО ПОНЯТИЯ — против AR-143.
PAIRED_HOURS_ODD / PAIRING_HOURS_ODD; PAIRED_FORBIDDEN_FIRST_GRADE /
PAIRING_FORBIDDEN_FIRST_GRADE; TEACHER_UNAVAILABLE_OVERBOOKED / TEACHER_TIME_SHORT;
PRIORITY_START_OVERFLOW / PRIORITY_RANK1_OVERFLOW; CALENDAR_NOT_READY /
CALENDAR_YEAR_MISSING. Ты вносишь только свои три и НЕ ПЛОДИШЬ новых пар. Сведение
пар — отдельная работа, назови её в отчёте как оставшуюся.

ТРЕТЬЯ: текст TEACHER_TIME_SHORT (schedule-parameters.ts:434) до сих пор говорит
«методический день И ОБЕД оставляют меньше места» — ссылается на норму, убранную
редакцией 6. Поправь текст, кода не трогай.

ДОКАЗАТЕЛЬСТВО: npm --workspace apps/api run texts:check — 0 отказов, в выводе
множества кодов сходятся в обе стороны.
```

---

## Волна 2 · один агент, строго последовательно

### А-Э3+Э4 · Ядро: самопроверка и улучшение расстановки

```
ЗАДАЧА: Э3, затем Э4 из specs/schedule-block/50-implementation.md.
ЭТО ГЛАВНЫЙ ЭТАП ВСЕЙ РАБОТЫ. Владелец сформулировал ценность прямо: расписание
должно стать ТЕМ, ЧТО НА МАКЕТЕ, а не первой попавшейся допустимой сеткой.

Э3 — МОСТ И САМОПРОВЕРКА:
1. Приватный buildQualityContext(input: GenInput): QualityContext рядом с buildInput
   (schedule.service.ts:236). Построчный образец УЖЕ ГОТОВ — apps/api/scripts/quality-check.ts:111-124.
2. В generate() МЕЖДУ `const res = generate(input)` (:308) и созданием шаблона (:315):
   unitsFromSlots(res.slots, ctx) → invariants(units, ctx).
3. При непустом ответе: шаблон НЕ СОЗДАЁТСЯ, зерно и параметры в лог, бросается
   SchoolError('INVARIANT_BROKEN', { invariant: invariantTitle(v[0].code), address }).

ПОРЯДОК «СНАЧАЛА ПРОВЕРКА, ПОТОМ ЗАПИСЬ» ОБЯЗАТЕЛЕН. Отказ, брошенный ПОСЛЕ создания
черновика, выпустит наружу сетку с нарушением — прямо против §5 спеки.

ОТДЕЛЬНАЯ РАБОТА Э3, САМАЯ ВАЖНАЯ ПО ПОСЛЕДСТВИЯМ:
I-1 в реализации ловит ТОЛЬКО ДУБЛИКАТ (quality.ts:144-149). Эталон ловит и ПОТЕРЯННЫЙ
ЧАС (specs/schedule-block/model/quality.mjs:176-178: две проверки — «единица не размещена»
и «размещена несуществующая единица»). Причина: QualityContext не несёт ожидаемой
нагрузки, а unitsFromSlots строит единицы ИЗ РАССТАНОВКИ, поэтому «что должно было быть»
неизвестно. Ворота G-57 подделывают I-1 только дублированием (quality-check.ts:186),
поэтому дыра зелёная.
СЕГОДНЯ ПРОПАВШИЙ УРОК ПРОХОДИТ АВТОПРОВЕРКУ. Это прямо против AR-144 («годовая норма —
закон сборки»). Мост обязан нести ожидаемый состав единиц из GenInput.pairs, а I-1 —
проверять ОБЕ СТОРОНЫ. Добавь в quality-check.ts подделку «час пропал».

Э4 — УЛУЧШЕНИЕ РАССТАНОВКИ В БОЮ:
Цепочка: ctx → unitsFromSlots → invariants → repair(units, ctx, REPAIR_BUDGET)
→ invariants(fixed.units) → penalties/lowerBound/qualityDto.
В templateSlot.createMany (:325) пишутся slotsFromUnits(fixed.units) С origin,
А НЕ res.slots — ЭТО И ЕСТЬ ТОЧКА ПОДКЛЮЧЕНИЯ.
В scheduleTemplate.create (:311) ложатся penalty, penaltyFloor, status: 'repaired'.
SchedulePreviewDto (schoolium.ts:476) получает quality: ScheduleQualityDto и traded.

ТЕМ ЖЕ ЭТАПОМ снимается ТРОЙНОЙ счёт одного и того же: generator.ts:327-333 и
schedule.service.ts:379-393 считают «приоритетный предмет позже середины дня» двумя
разными кусками кода, правило prio — третий раз. Источник остаётся ОДИН.

ЛОВУШКА ПО ВРЕМЕНИ: repair() пересчитывает neighbourhood + invariants + penalties на
КАЖДЫЙ ход-кандидат (quality.ts:596-601) — 1288 кандидатов на шаг, каждый полный проход
по 112 часам. Изолированного замера repair() на 112 часах в 40-bench.md НЕТ.
СНАЧАЛА ЗАМЕРЬ, потом подключай. Если синхронная ручка близка к таймауту — скажи это
числом в отчёте, не «подозреваю». МУЛЬТИСТАРТ (Э9) НЕ ВКЛЮЧАЙ НИ В КАКОМ ВИДЕ: он идёт
после Э8, иначе повесит ручку и уронит три ворот на БД по таймауту.

ВТОРАЯ ЛОВУШКА: без Э1 (origin в БД) признак правки человека теряется на первом чтении,
и следующий прогон вернёт подвинутый урок назад (neighbourhood исключает часы именно
по origin, quality.ts:503). Убедись, что Э1 влит.

ТРЕТЬЯ: статус 'repaired' — НОВЫЙ. school-state.service.ts:83-85 читает статус
ПЕРЕЧИСЛЕНИЕМ ТРЁХ ЛИТЕРАЛОВ (confirmed→ready, stale→stale, draft→generated), всё
прочее проваливается на лестницу :92 и вернёт day_params_set — то есть школа выпадет
из ready и У ВСЕХ ПРОПАДЁТ ЖУРНАЛ. Роняет G-33 и смоки G-53/G-55.
Правишь ОДНИМ КОММИТОМ: school-state.service.ts, SchedulePreviewDto.status
(schoolium.ts:478, union из трёх литералов), states.mjs, и два запроса
deleteMany({status:'draft'}) (schedule.service.ts:315 и :348) — они перестанут
находить черновики со статусом repaired.

ДОКАЗАТЕЛЬСТВО:
npm --workspace apps/api run onboarding:check
npm --workspace apps/api run regen:check
npm --workspace apps/api run concurrent:check
npm --workspace apps/api run quality:check
node specs/schedule-block/model/quality.mjs
Плюс НОВАЯ проверка в apps/api/scripts/regeneration-check.ts: сетка, отданная живой
generate(), проходит invariants() на реальных данных — сегодня это доказано только
на чистых функциях.
В логе сборки должно быть видно цену сетки ДО и ПОСЛЕ и число принятых ходов.
Измеренный ориентир первой школы: 2080 → 1128 за 25 ходов, граница 632.
```

---

## Волна 3 · два агента параллельно

### А-Э5 · Опора на подтверждённую сетку

```
ЗАДАЧА: Э5. Зависит от Э1 и Э4.

До deleteMany({status:'draft'}) (schedule.service.ts:315) читаешь шаблон со
status='confirmed' ЯВНО — не «последний по generatedAt», как делает preview() (:355).
Собираешь Map<unitId,{dayNo,slotNo}>, передаёшь в penalties() и repair().
qualityDto(pen, true, floor): без подтверждённой сетки правило показывается
НЕАКТИВНЫМ, а не единицей (quality.ts:461-464).

ЛОВУШКА: вся ценность этапа держится на устойчивом идентификаторе часа (Э1, orderBy).
Сравнение по разъехавшимся идентификаторам даёт шум вместо измерения. Проверь, что
Э1 влит, прежде чем начинать.

ДОКАЗАТЕЛЬСТВО: npm --workspace apps/api run regen:check плюс НОВАЯ проверка: после
подтверждения и регенерации правило устойчивости активно, и число сместившихся часов
МЕНЬШЕ размера сетки. Сегодня регенерация сдвигает 108 часов из 112 — это находка
стенда, а не норма.
```

### А-Э6 · Ручка чтения удобства и панель на экране

```
ЗАДАЧА: Э6. Зависит от Э4.

1. GET /api/v1/schedule/quality в ScheduleController (schoolium.controllers.ts:365),
   @RequirePermission('schedule.read') — право уже в каталоге.
2. Метод quality(templateId?) в сервисе поверх готовой цепочки Э4.
3. Запись маршрута в SCHOOL_API (packages/shared/src/schoolium.ts:712).
4. ПЕРЕВОД КЛИЕНТА НА SCHOOL_API: сегодня grep SCHOOL_API по apps/web/src даёт НОЛЬ
   совпадений — пути собираются строками (apps/web/src/schoolium/api.ts:156-167).
   Пополнение реестра само по себе ни на что не влияет, пока клиент не переведён.
5. Панель на S-40 (apps/web/src/schoolium/screens/schedule.tsx): агрегат, ПОТОЛОК
   РЯДОМ, семь правил, размен списком.

ЛОВУШКА ТЕРМИНОЛОГИИ: G-62 сегодня НЕ СМОТРИТ НА JSX — ворота проверяют только
GLOSSARY, QUALITY_MARKER_TITLES, INVARIANT_TITLES, подписи SCHEDULE_PARAMS и тексты
отказов (quality-check.ts:464-489). Ни одной строки из apps/web они не видят. Значит
жаргон на экране пройдёт молча. Расширь G-62 на строковые литералы schedule.tsx.

ВТОРАЯ: AR-128 — агрегат БЕЗ ПОТОЛКА прочитать нельзя. «88 %» само по себе не значит
ничего. Потолок стоит рядом всегда, размен показывается списком, а не прячется.

ТРЕТЬЯ: правило 4 ворот G-52 (tools/method/check-testids.mjs:131-138) — модуль,
несущий идентификатор чужого экрана, это отказ. Идентификаторы панели относятся к
S-40, который уже существует; чужих не заводи.

ДОКАЗАТЕЛЬСТВО: npm --workspace apps/api run authz:check; quality:check без изменений;
npm run method:lint; ручка отдаёт СЕМЬ строк и потолок.
```

---

## Волна 4 · по одному агенту, последовательно

### А-Э7 · Ручной ход, отмена, четыре события

```
ЗАДАЧА: Э7. Зависит от Э1, Э2, Э4.

1. Модель журнала ходов в schema.prisma рядом с TemplateSlot + ЗАПИСЬ В
   apps/api/src/common/tenant/tenant-models.ts (иначе таблица не фильтруется по школе).
2. POST /schedule/repair, /schedule/move, /schedule/move/undo. Каждый несёт версию
   агрегата, вызывает state.checkVersion('schedule', …) и state.bump В ОДНОЙ ТРАНЗАКЦИИ
   (school-state.service.ts:38-62).
3. evaluateManualMove ставит признак 'manual'.
4. При подтверждённой сетке ход запускает ТУ ЖЕ переработку уроков, что confirm
   (schedule.service.ts:404-476): урок с отметками отвязывается, пустой исчезает.
5. Четыре события: schoolium.contract.ts:13, EVENT_CONTRACT:119, AUDIT_LABELS:153,
   карта AUDITED (apps/api/src/common/audit/audit.service.ts:18), таблица
   specs/school-onboarding/30-spec.md:25 И ЧИСЛО 22 → 26 в
   apps/api/scripts/event-subscribers-check.ts:43-44.

ГЛАВНАЯ ЛОВУШКА ЭТОГО ЭТАПА — ХОД MOVE НЕ УМЕЕТ ТОГО, ЧТО ТРЕБУЕТ ЭКРАН:
neighbourhood порождает MOVE ТОЛЬКО в позицию n+1 — первую свободную в конце дня
(quality.ts:503-513). I-5 требует, чтобы занятые позиции дня класса образовали
префикс 1…k (quality.ts:177-192). Значит перенос часа ИЗ СЕРЕДИНЫ дня рвёт префикс
исходного дня и отклоняется ВСЕГДА. Реально применимы только перенос последнего
урока дня и SWAP внутри класса. Экран «перетаскивание» на этом контракте отдаст
MOVE_REJECTED почти на каждый жест.
РЕШЕНИЕ (твоё, не вопрос владельцу — AR-141): ручной ход получает ОТДЕЛЬНУЮ СЕМАНТИКУ —
он УПЛОТНЯЕТ день-источник (сдвигает хвост вверх) и раздвигает день-приёмник, сохраняя
I-5 КОНСТРУКЦИЕЙ ХОДА, а не отказом. Окрестность АВТОМАТИЧЕСКОГО поиска остаётся
суженной: её сужение — измеренное решение (40-bench.md, находка 3), не трогай его.

ВТОРАЯ ЛОВУШКА: TemplateSlot.@@unique([templateId, dayNo, slotNo, classId, groupNo])
(schema.prisma:1374). Обмен двух уроков двумя последовательными UPDATE нарушит
уникальность на первом же. Пиши обмен ОДНОЙ ТРАНЗАКЦИЕЙ через временное значение
либо deleteMany + createMany.

ТРЕТЬЯ: G-41 (moderator-audit-check.ts:147-155) требует, чтобы КАЖДАЯ строка
EVENT_CONTRACT была И в AUDITED, И в AUDIT_LABELS. Событие, добавленное только в
контракт, роняет ворота. G-50 хардкодит число 22 в ДВУХ местах.

ЧЕТВЁРТАЯ: ход, правящий только TemplateSlot без переработки уроков, роняет G-42
(регенерация не теряет историю). Отметка не удаляется правкой расписания ни при
каком ходе.

ДОКАЗАТЕЛЬСТВО: subscribers:check, moderator:check, regen:check, concurrent:check —
все зелёные, числами.
```

### А-Э8 · Сборка как задача: jobId и опрос

```
ЗАДАЧА: Э8. Зависит от Э4. САМЫЙ ШИРОКИЙ ПО ПОСЛЕДСТВИЯМ ЭТАП.

1. Модель задачи в schema.prisma + tenant-models.ts.
2. POST /api/v1/schedule/generate (schoolium.controllers.ts:405) отдаёт jobId немедленно.
3. Ручка состояния, опрос раз в 2 секунды — ТЕМ ЖЕ способом, что ожидание скана QR
   (AR-87), без WebSocket. GenerationJobDto уже готов (schedule-parameters.ts:448).
4. Отмена оставляет школу в day_params_set.
5. Шаблон пишется ОДНОЙ транзакцией в конце.

ТЕМ ЖЕ КОММИТОМ правишь ВСЕХ синхронных потребителей, иначе трое ворот на БД и два
браузерных смока красные:
   apps/api/scripts/onboarding-check.ts:153
   apps/api/scripts/regeneration-check.ts:73
   apps/api/scripts/concurrent-edit-check.ts:111
   apps/web/src/schoolium/api.ts:160
   apps/web/src/schoolium/screens/schedule.tsx — три вызова (:54, :350, :797)

ОКНО СБОРКИ — АНИМАЦИЯ БЕЗ ЕДИНОЙ ЦИФРЫ. PROGRESS_SHOWS_NUMBERS = false, AR-138.
Первая редакция AR-130 требовала «перезапуск 7 из 20, лучшее качество 88 %» — она
ВЫТЕСНЕНА, и в реестре это помечено. Владелец отверг цифры дословно: экран видит
человек без технического образования. Не воскрешай их ни в каком виде.

ДОКАЗАТЕЛЬСТВО: onboarding:check, regen:check, concurrent:check зелёные после правки
скриптов; npm --workspace apps/web run smoke:onboarding (49 шагов) и мобильный смок
зелёные.
```

---

## Волна 5 · три агента параллельно

> Общий файл у всех трёх — `packages/shared/src/schoolium.ts`. Согласуй очередь
> правок этого файла до старта, иначе конфликты.

### А-Э9 · Мультистарт по глубине поиска

```
ЗАДАЧА: Э9. Зависит от Э8 и Э4.

SchoolState.searchDepth String @default("normal"); поле depth в GenInput
(generator.ts:59) и чтение в buildInput; цикл вариантов вокруг «сборка + улучшение»
с SEARCH_DEPTHS (5/30/200, ранняя остановка 2/3/6 — schedule-parameters.ts:239,
сегодня единственная ссылка на эту константу во всём репозитории — ворота);
выбор наименьшей цены; сегмент «Быстрый · Стандартный · Тщательный» на экране.

ЛОВУШКА: шаг зерна внешнего цикла ОБЯЗАН отличаться от input.seed + restart*7919 —
ровно этот шаг использует ВНУТРЕННИЙ рестарт генератора (generator.ts:291). Зёрна
пересекутся, и часть вариантов повторит уже просмотренные.

ВТОРАЯ: без потолка по времени 200 вариантов на школе в 32 класса не завершатся.
Приоритет «бюджет против числа вариантов» назови явно в коде и в реестре решений.
GENERATOR_BUDGET (schoolium.ts:186) остаётся страховкой и НЕ заменяется числом вариантов.

ТРЕТЬЯ: счётчики вариантов — в лог и аудит. Человеку — ни одной цифры.

ДОКАЗАТЕЛЬСТВО: daygrid:check, generator:check, onboarding:check зелёные; лог
показывает опробованные варианты и лучшее зерно. Измеренный ориентир: 20 вариантов
дают Π = 1022 против 1128 одиночного прогона (минус 21 % зазора).
```

### А-Э10 · Приоритет рангом 1…6

```
ЗАДАЧА: Э10. Зависит от Э4. ВОПРОСОВ ВЛАДЕЛЬЦУ НЕТ — все закрыты (§6).
Приоритет — шкала 1…6, названная владельцем: «приоритет, где первый — самый
главный», «математика имеет первый приоритет, то есть обязана идти 1 или 2 уроком».
Приоритет 1 — СИЛЬНОЕ требование, но оно не вправе оставить школу без расписания
(«без no solution, дети не могут остаться без урока»): при невыполнимости отвечает
ступень диагностики С ДЕЙСТВИЕМ, а не отказ. Настройку расписания ведёт МОДЕРАТОР
(AR-148).

priorityRank Int @default(3) РЯДОМ с существующим priority Boolean (expand → migrate →
contract), миграция переносит true → 1, false → 3. Дальше: SubjectDto, SetPrioritiesDto,
запись (schedule.service.ts:178), маршрут, клиент, экран, вход движка (generator.ts:47,
113, 244, 256), правило порядка по неубыванию ранга с inversionCost / PRIORITY_WEIGHT.
QualityContext.priority (quality.ts:78) из Set<string> становится отображением
«предмет → ранг» — ВМЕСТЕ С ФИКСТУРОЙ ворот (quality-check.ts:88-124).

ГЛАВНАЯ ЛОВУШКА: фикстура firstSchool(20260826) — это основание ВСЕХ ворот G-56…G-60.
Математика и русский стоят по 4 часа в каждом из 8 классов. При ЖЁСТКОМ ранге 1 она
может перестать собираться и уронит G-56…G-60 РАЗОМ. ПРОГОНИ ФИКСТУРУ ДО перевода
требования в жёсткое, и если она не собирается — значит требование обязано
отвечать ступенью диагностики с действием, а не отказом. Фикстуру не подгонять.

ВТОРАЯ: сегодня приоритет объявлен МЯГКИМ прямо в шапке generate() (generator.ts:265-272,
пункт 6 «МЯГКОЕ: помечается»), а реестр параметров делает уровень 1 ЖЁСТКИМ (H15).
Разведи так: требование сильное, но исчерпание отвечает ступенью диагностики
с действием, а не NO_SOLUTION.

ТРЕТЬЯ: признак prioritiesSet (schema.prisma:1496, читается school-state.service.ts:101)
при дефолте 3 перестаёт отличать «заданы» от «не трогали». Переопредели явно, иначе
мастер онбординга сломается.

ЧЕТВЁРТАЯ: миграция ОБЯЗАНА назначить ранги существующим записям, иначе правило
порядка станет пустым.
```

### А-Э12 · Выдача наружу

```
ЗАДАЧА: Э12. Зависит от Э1, Э2, Э4.
ВРЕМЯ НАЧАЛА ДНЯ УЖЕ ЕСТЬ: DaySkeleton.startTime (packages/shared/src/schedule-parameters.ts),
у каждого скелета своё. НЕ ЗАВОДИ своё поле — бери из скелета дня.

Право schedule.share в apps/api/src/common/authz/catalog.ts и в пакеты ролей;
модели ScheduleSnapshot и ScheduleShare + tenant-models.ts; маршруты POST /schedule/snapshot,
POST /schedule/snapshot/:id/revoke, GET /schedule/export, публичная GET /s/:token;
пять кодов выдачи порядком Э2.

ЛОВУШКА, ПОСЛЕ КОТОРОЙ ПРАВКА НЕВОЗМОЖНА: scopeSlots для области 'teacher' фильтрует
по ИМЕНИ ПЕДАГОГА, а не по идентификатору (quality.ts:732), тогда как §11.1 задаёт id=…
Имя попадает в подписываемую строку и в адрес. Если выдать ХОТЬ ОДНУ ссылку до перевода
на идентификатор — правка станет невозможной без обрыва выданных ссылок, а однофамильцы
получат чужие уроки. ПЕРЕВЕДИ ДО ПЕРВОЙ ССЫЛКИ.

ВТОРАЯ: секрет подписи не назван нигде — signSnapshot принимает его параметром
(quality.ts:721), переменной среды и записи в .env.example нет. Заведи и назови.

ТРЕТЬЯ: ScheduleSnapshot НЕ ВЕШАЕТСЯ каскадом на ScheduleTemplate — confirm() удаляет
прежние шаблоны (schedule.service.ts:473). Снимок переживает подтверждение новой сетки.
workspaceId входит в интерфейс снимка И в подписываемую строку (§9.1 его перечисляет,
а schedule-quality.ts:232 — нет).

ЧЕТВЁРТАЯ: SnapshotMeta.version — НЕ reg.scheduleVersion (school-state.service.ts:40):
она растёт при любой правке нагрузки и отзовёт ссылки там, где сетка не менялась.

ПЯТАЯ, ПОЧИНКА: projectIcs считает время урока как startMinutes + (slotNo−1)·(lessonMin +
breakMin) (quality.ts:774-777) — БОЛЬШАЯ ПЕРЕМЕНА В РАСЧЁТ НЕ ВХОДИТ, поэтому после
второго урока время события расходится со звонками. G-60 сверяет только число ячеек,
поэтому ворота зелёные. Почини формулу и добавь в G-60 сверку времени, а не только счёта.

ШЕСТАЯ: публичный маршрут /s/:token идёт с системным контекстом
(tenant.interceptor.ts:40, tenantId: null) — ограничь выборку школой ЯВНО в запросе.
Глобальный префикс 'api' (main.ts:9) — маршрут выводится из-под него явно.

ДОКАЗАТЕЛЬСТВО: authz:check зелёный; texts:check зелёный; quality:check раздел G-60;
ссылка области «педагог» не отдаёт чужие уроки — проверка на реальных данных.
```

---

## Волна 6 · два агента параллельно

### А-Э11 · Спаренность уроков

```
ЗАДАЧА: Э11. Зависит от Э10.

SchoolSubject.pairing Int @default(5) (DEFAULT_PAIRING); поле в SubjectDto; шаг мастера;
запись. В движке единица планирования становится блоком из ОДНОГО ИЛИ ДВУХ смежных
часов; число блоков выводится из уровня и PAIRING_TOLERANCE.

ПЕРВАЯ ЛОВУШКА, ОНА ЖЕ САМАЯ ДОРОГАЯ: слово «спаренный» в generator.ts:18-21, :107, :254
означает ДРУГОЕ — все группы предмета в ОДНОМ слоте (AR-75), а не два смежных часа.
«Доработка существующей спаренности» СЛОМАЕТ атомарность групп (H4). Это два разных
понятия с одним словом; заведи для нового своё имя.

ВТОРАЯ: правило удобства subjectSpread (quality.ts:349, важность 6) наказывает два часа
одного предмета в один день. Это РОВНО ТО, чего требует уровень спаренности 1 («строго
обязательно»). Не разведя их, движок будет считать каждую правильно поставленную пару
нарушением. Разведи явно.

ТРЕТЬЯ, УЖЕ ИСПРАВЛЕНО ДО ТЕБЯ: pairingIsAdjacent несла вторую ветвь, бывшую
ПОДМНОЖЕСТВОМ первой, — условие выглядело содержательным, ничего не проверяя.
Сведена к смежности по номеру позиции: большая перемена пару НЕ РАЗРЫВАЕТ, потому
что позиции 2 и 3 смежны независимо от того, что между ними. Ворота это ловят.

ЧЕТВЁРТАЯ: спаренность из приоритета НЕ выводится (владелец: «явно говорю: нет»).
Она ВЫВОДИТСЯ ИЗ ГОДОВЫХ ЧАСОВ — AR-147, функция pairingFromYearHours уже написана
и покрыта воротами G-61 (монотонность, границы, невыводимость уровня «запрещено»).
Твоя работа — ЧИТАТЬ её в движке, а не выдумывать свою. Человек вправе изменить
выведенное: вывод — дефолт, а не запрет.
```

### А-Э13 · Годовая норма, добор, подстраховка

```
ЗАДАЧА: Э13. Зависит от Э8 и календаря. Читай §7.5 35-parameters.md и §7.1 30-spec.md
ЦЕЛИКОМ, плюс AR-144, AR-145, AR-146 дословно.

Строишь: годовые часы пары «предмет × класс»; недельные становятся ВЫВОДОМ из годовых
и числа учебных недель; модели счёта «положено минус проведено» и режима подстраховки
(типы готовы — schedule-parameters.ts:285-334); пять кодов порядком Э2; ручки чтения
долга, принятия предложения добора и состояния подстраховки — ИХ НЕТ НИ В КОДЕ,
НИ В §11.1 СПЕКИ, дыру контракта закрываешь ты.

ЛОВУШКА 1, ЧАСЫ ИСЧЕЗАЮТ РАНЬШЕ, ЧЕМ ИХ УСПЕВАЮТ СОСЧИТАТЬ:
cascadeUnbind (staff.service.ts:307) → unbindAllOfTeacher (subjects.service.ts:335)
делает deleteMany по TeacherBinding — вместе со строкой ФИЗИЧЕСКИ ИСЧЕЗАЕТ hoursPerWeek.
К моменту включения подстраховки считать недобор уже не из чего. Снимай часы ДО каскада.

ЛОВУШКА 2, ДВА ОТВЕТА НА ОДНО СОБЫТИЕ:
staffDeactivated и staffDeleted УЖЕ стоят в STALE_ON_EVENTS (schoolium.contract.ts:189),
подписчик markStale (schedule.service.ts:100) роняет сетку в stale, и экран показывает
«расписание устарело». Режим подстраховки обязан развести это: сетка не устарела,
она в подстраховке.

ЛОВУШКА 3, ТРИГГЕР НЕ ТОТ:
staff.remove() для педагога с историей ЗАПРЕЩЁН гейтом STAFF_HAS_HISTORY
(staff.service.ts:385): реально ведший педагог удалению не подлежит вовсе.
ТРИГГЕР ПОДСТРАХОВКИ — ДЕАКТИВАЦИЯ, а не удаление.

ЛОВУШКА 4, ДОБОР НЕ ПОМЕЩАЕТСЯ В СЕТКУ:
час добора — ЛИШНИЙ час сверх недельной нормы. I-1 и H1 требуют «посторонних размещений
нет». Положишь его в TemplateSlot — инвариант падает и сетка не выйдет наружу. Ключ
идемпотентности материализации SchoolLesson.@@unique([workspaceId, date, slotNo,
classId, groupNo]) (schema.prisma:1396) расширять НЕЛЬЗЯ: на нём стоят AR-101 и G-45.
ДОБОР ЖИВЁТ ОТДЕЛЬНОЙ МОДЕЛЬЮ, не строкой шаблона.

ЛОВУШКА 5, СНЯТИЕ ПРЕДМЕТА РВЁТ ПРЕФИКС:
I-5 требует, чтобы занятые позиции дня класса образовали префикс 1…k. Снятие предмета
ИЗ СЕРЕДИНЫ дня без сдвига хвоста создаёт окно и валит I-5; сдвиг хвоста двигает уже
материализованные уроки, у которых могут быть отметки. Пересборка дня идёт тем же
перебором, что полная сборка, и переработка уроков — та же, что в confirm.

ЛОВУШКА 6, ПРОШЛОГО НЕТ:
materialize() стартует с max(сегодня, начало 1-й четверти) (schedule.service.ts:496) —
прошлое не материализуется никогда. Если сетку подтвердили в октябре, уроков за сентябрь
в SchoolLesson нет. «Проведено на дату» за прошедшие недели считается ПО ЖУРНАЛУ.

ТЕМ ЖЕ ЭТАПОМ — СТУПЕНЬ 2 ДИАГНОСТИКИ вместо NO_SOLUTION (§7 35-parameters.md):
повторные сборки со снятыми ПО ОДНОМУ требованиями (RELAXABLE, schedule-parameters.ts:418)
с ответом-ДЕЙСТВИЕМ («собирается, если перенести методический день … Перенести?»).
ЛОВУШКА: G-46 (day-grid-check.ts:158) и G-34 (generator-check.ts:168) СЕГОДНЯ ТРЕБУЮТ,
чтобы исчерпание бюджета отвечало именно NO_SOLUTION. Это прямое противоречие с реестром
параметров. Разведи оба и поправь ворота вместе с кодом, одним коммитом.

ДОКАЗАТЕЛЬСТВО: quality:check раздел G-63 остаётся зелёным и ДОПОЛНЯЕТСЯ проверками
на реальных данных; calendar:check зелёный; предложение добора называет предмет, класс,
ДАТУ и НОМЕР УРОКА. Подключение к БД не должно изменить НИ ОДНОГО ТЕКСТА — они
проверяются дословно и проходят G-62.
```

---

## Волна 7 · последовательно

### А-Э14 · Ворота в CI

```
ЗАДАЧА: Э14.

1. quality:check входит в schoolium:check (apps/api/package.json) И в CI отдельным job.
   СЕГОДНЯ ОН НЕ БЕЖИТ НИГДЕ: grep quality по .github/workflows/ci.yml даёт ноль.
   97 зелёных утверждений существуют только когда их запускают рукой.
2. ЛОВУШКА: quality-check.ts:83 импортирует { check, report } из './schoolium/harness',
   а harness.ts на верхнем уровне тянет AppModule, PrismaService и соседние сервисы.
   «Ворота чисты от БД» верно про их СОДЕРЖАНИЕ, но не про запуск. Раздели стенд на
   чистую часть и часть с Prisma, чтобы job шёл без БД.
3. Новые ворота на РЕАЛИЗАЦИЮ, а не на спеку:
   G-64 — сетка, отданная живой ручкой, проходит invariants(); шаблон с нарушением
          в БД не создаётся;
   G-65 — записанная сетка улучшена: penalty шаблона строго меньше цены первой
          допустимой; origin доехал до БД и до предпросмотра;
   G-66 — реестр параметров ЧИТАЕТСЯ ДВИЖКОМ: изменение ранга меняет расстановку.
          Сегодня G-61 перечисляет свойства самих констант и совместим с НУЛЁМ
          использований реестра в бою;
   G-67 — число параметров и шагов в документах равно фактическому.
4. Строки в docs/G-CHECKS.md, AR-строки при необходимости, номера — из шапки
   G-CHECKS.md, не выдумывать.
```

### А-Э15 · Экраны

```
ЗАДАЧА: Э15. Зависит от Э6, Э7, Э8, Э12.

S-43 и S-44 ЗАВОДЯТСЯ С НУЛЯ: grep по 70-screens.md, 75-adaptive.md и apps/web/src
даёт для них НОЛЬ совпадений. Каждый экран обязан получить: маршрут и роли
(lint-screens.mjs:27), раскладки в 75-adaptive.md §6 (G-39), идентификаторы элементов
и запись в карте модулей (G-52, check-testids.mjs:131 — модуль с чужим идентификатором
это отказ), коды отказов не менее двух упоминаний в описании (lint-screens.mjs:63).

Плюс §8 спеки — графическая маркировка: четыре слоя, метка происхождения в углу ячейки,
штриховка stale, перечёркивание отвязанного урока, правило «цвет не единственный
носитель смысла».

ЛОВУШКИ:
· G-37 (check-contrast.mjs:63) сканирует РОВНО apps/web/src/schoolium. Второй экран
  расписания — apps/web/src/app/screens/ScheduleScreen.tsx — лежит ВНЕ зоны и содержит
  литеральные цвета. Либо приведи к токенам, либо расширь зону ворот.
· Класс .sch-week объявлен ДВАЖДЫ в apps/web/src/schoolium/app.css (:350 чип недели
  журнала, :411 контейнер сетки расписания). Разведи имена.
· G-38 (lint-screens.mjs:20) читает specs/school-onboarding/30-spec.md, а НЕ
  specs/schedule-block/30-spec.md. Событие, названное в новой секции 70-screens.md,
  сверяется с таблицей онбординга.
· Прогресс на S-42 — БЕЗ ЕДИНОЙ ЦИФРЫ.
```
